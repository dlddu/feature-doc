import { CredentialsSetup } from './CredentialsSetup';

// Walking skeleton has a single screen: S01 Credentials Setup. As more screens
// land this becomes a router; for now it renders S01 directly.
export function App() {
  return <CredentialsSetup />;
}
