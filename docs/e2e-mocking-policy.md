# E2E 모킹 정책

> 정책·허용목록·차단 요인 원장의 SSOT. 2026-09-17 최초 등재 (reconciler `rct_20260917-0001`, 모델 `tbm_feature-doc-e2e-mock-policy`).
> `docs/doc-tracker/`의 「e2e 매핑」 절은 갱신이 잦은 로드맵·이력 축이므로 이 문서와 분리된다(같은 결정을 한 문서로 모으기 위한 전용 문서).

## 목적과 적용 범위

이 레포의 E2E는 **kind 실클러스터에 로컬 빌드 이미지로 API와 분석 워커(별도 Deployment)를 배포하고, 실 SQLite(PVC)·실 봉투 암호화·실 워커 큐를 그대로 구동하는 하네스**(`scripts/e2e.sh` → `deploy/e2e/` 오버레이 → `e2e/smoke.sh` + Playwright)가 기본이다. 경계별 `FEATUREDOC_DOUBLE_*` env가 켜는 인프로세스 테스트 더블(`Mode::Stub` 분기)과 브라우저 네트워크 인터셉트는 **실환경으로 재현이 불가능한 경우에 한해서만** 허용하고, 그 예외는 이 문서에 등재된 것만 인정한다. 편의(실환경 준비 회피, 어서션 단순화, 플레이키 무마)를 위한 모킹은 drift다.

## 모킹으로 세는 것 (범위 경계)

이 레포의 치환은 네트워크 층이 아니라 **제품 바이너리 안**에 있다. 다음 셋을 모킹으로 센다:

1. **더블 활성 배선** — 아래 5개 env 중 하나를 `stub`으로 켜는 자리(`deploy/e2e/kustomization.yaml`의 API·워커 패치). 전역 모드 스위치는 2026-09-18 제거됐다(아래 「stub mode 제거 — 완료 기록」). 배선은 **모킹 지점이 아니다**: 등재 대상은 그것이 선택하는 코드 분기이고, 표기 규약대로 활성 지점에는 예외 주석을 달지 않는다.

   | env | 경계 | CODE | 읽는 프로세스 |
   |-----|------|------|---------------|
   | `FEATUREDOC_DOUBLE_GITHUB_AUTH` | 로그인 신원 | EXT-01 | API |
   | `FEATUREDOC_DOUBLE_GITHUB_APP` | App 설치·설치 토큰·저장소 목록 | EXT-02 | API |
   | `FEATUREDOC_DOUBLE_LLM_KEY` | LLM 키 라이브 검증 | EXT-04 | API |
   | `FEATUREDOC_DOUBLE_REPO_SCAN` | 저장소 트리 스캔 · 핵심 파일 본문 읽기 | EXT-03 | API · 워커 |
   | `FEATUREDOC_DOUBLE_LLM` | LLM 호출·고정 답 공급 | LLM-01 | API · 워커 |

   각 env는 **그 경계를 소유한 프로세스만** 읽는다. LLM·트리 스캔 두 경계는 두 프로세스가 소유한다 — 분석 파이프라인은 워커가, AC3.1 편집 제안과 AC3.2 빠진 feature 추가의 근거 찾기는 큐를 타지 않고 API가 직접 모델을 부르므로(2026-09-21, #92·#107) **같은 이름을 각자의 프로세스에서** 읽고, 근거 찾기가 워커의 1단계와 같은 트리 스캔을 API에서 한 번 더 하므로(워커가 본 경로 목록은 저장되지 않는다) `FEATUREDOC_DOUBLE_REPO_SCAN`도 같은 방식으로 공유된다(#107). 값이 아니라 이름을 공유하는 것이라 한쪽만 stub인 배포가 가능하고, 오버레이의 API 패치와 워커 패치가 이 env를 따로 켠다. 그 밖의 경계는 API Deployment에 워커 더블을 켤 수단이 없고 그 역도 같아서, 「하나를 켜면 전부 켜진다」는 성질이 배선 수준에서 사라졌다. 미설정은 언제나 실연동이므로 아무 말도 하지 않는 배포는 아무것도 stub하지 않는다.
2. **stub 분기** — `Mode::Stub` 분기가 외부 경계를 결정적 더블로 대신하는 자리(GitHub 로그인 신원 · App 설치와 설치 토큰 · 접근 가능 저장소 목록 · 저장소 트리 스캔 · LLM 호출 · LLM 키 실검증), 그리고 LLM 단계가 고정 답을 공급하는 자리(`Ask { stub: stub_*(...) }` — 현재 `stub_answer` · `stub_logic` · `stub_tests` · `stub_dependencies` · `stub_edit` · `stub_draft` · `stub_merge`).
3. **브라우저 네트워크 인터셉트** — Playwright `page.route`·`route.fulfill` 계열(현재 0건).

SQLite·봉투 암호화·세션·워커 큐·워커 replica 임대(`e2e/support/cluster.ts`의 `kubectl scale`)는 **실제 구현이 도는 것**이라 모킹이 아니다. stub 신원 선택(`/api/auth/login?as=<handle>`)은 로그인 분기의 입력이라 별도 지점으로 세지 않는다.

## 허용 카테고리 (이 밖은 불허)

- **`EXT` — 외부 계정·자격증명 경계**: 실 상류가 실제 GitHub 계정·App 설치·OAuth 동의 화면 또는 실 프로바이더 키를 요구해, kind CI 안에서 안전하게 구동하거나 호출할 수 없다. CI 시크릿으로 쓸 수 있는 전용 테스트 App·조직·키가 마련되면 해당 경계는 이 카테고리를 쓸 수 없고 실 상류로 대체해야 한다.
- **`LLM` — 비결정·과금 경계**: 실 LLM 응답은 비결정적이고 호출마다 과금돼, 산출물에 대한 결정적 단정이 성립하지 않는다. 산출물 **내용**과 무관한 단정(호출 여부·오류 전파·키 선택)만 필요한 경로에는 이 카테고리를 쓸 수 없다. 산출물의 의미적 품질은 이 정책의 대상이 아니다.

명시적 불허: 실제로 구동되는 구성요소(SQLite·봉투 암호화·세션·워커 큐)를 흉내 내는 치환, 플레이키 회피, **미구현 기능의 우회**(구현 대기 시나리오를 stub 응답으로 통과시키는 것 — 구현은 `tbm_feature-doc-docs-impl`의 몫), 어서션 단순화를 위한 치환, 그리고 **real 경로보다 관대한 stub 분기**. 카테고리 판정이 애매하면 제거 쪽으로 기운다 — 예외는 늘지 않는 방향으로만 관리한다.

**충실도 결함**(stub이 real보다 관대한 분기)은 등재 여부와 무관하게 drift이며, 등재로 덮지 않고 코드로 고친다. 현행 보증:

- `repo_scan::stub_scan`은 스텁 저장소 집합에 없는 브랜치를 real과 같은 `github tree rejected (404)`로 거부한다(2026-08-13 수정).
- `repo_scan::stub_read`는 같은 저장소·브랜치의 스텁 트리에 있는 경로에만 본문을 답하고, 그 밖의 경로는 real이 `404`를 건너뛰듯 건너뛴다 — 실 API가 줄 수 없는 파일을 2단계에 건네지 않는다(2026-09-24 등재와 함께).
- `llmkey` stub 검증은 길이 ≥ 20 **그리고** `provider.prefix()` 시작 형식만 받아들여, 미지원 제공자 키를 real과 같은 거부 사유로 막는다(2026-09-02 수정).
- `llm::stub_answer`는 `FEATUREDOC_STUB_LLM_FAIL`이 지정한 프롬프트 부분열이 ask 입력에 실재할 때 real과 같은 한도 초과 오류(`LLM rejected the request (429)`)로 실패하고, 그 밖의 입력은 결정적 답을 유지한다 — stub은 실 LLM의 실패(한도 초과·시간 초과)를 표현하지 못했던 원장 R1을 충실도 확장으로 닫은 것(2026-09-18, `rct_20260918-0001`).
- `bin/worker.rs`의 `provider_for`는 활성 LLM 키가 없는 job을 더블 활성 여부와 무관하게 real과 같은 사유(`no active LLM key for this user; register one to analyze`)로 거부한다. 이전에는 LLM 더블이 켜져 있으면 키 없는 job을 기본 제공자로 진행시켜, **분석의 진입 조건이 e2e에서만 느슨했다** — real보다 관대한 분기이므로 등재가 아니라 제거로 닫았다(2026-09-18, `rct_20260918-0002`). LLM 카테고리가 「키 선택」 경로를 덮을 수 없다는 조항과도 같은 결론이다. 이에 기대고 있던 `sc04-07`·`sc04-08`은 다른 분석 spec과 같은 키 등록 셋업을 갖췄다.

## 표기 규약

허용된 모킹 지점은 그 지점 직전 줄에 사유 주석을 단다 — Rust·TS는 `Mode::Stub` 분기나 인터셉트 호출 바로 위에 `//`, YAML은 스위치 env 바로 위에 `#`:

```
// mock-exception: <CODE> — <사유>
# mock-exception: <CODE> — <사유>
```

같은 항목이 이 문서의 허용목록에도 있어야 한다. 주석만 있고 미등재이거나, 등재만 있고 코드에 없으면 drift다.

주석은 **등재된 지점에만** 붙인다 — 미등재 지점(stub mode 제거 계획의 스위치 5쌍)에는 주석을 붙이지 않는다.

## 허용목록 (등재 집합 — 2026-09-21 증분 등재, 18쌍 / 15파일)

지점은 **파일 × 토큰 쌍** 단위로 등재한다. 아래 표가 등재 집합이며, 각 행의 CODE는 코드 주석에 동일하게 부착된다. 2026-09-21 AC3.1 편집 제안·AC3.2 빠진 feature 추가·AC3.5 충돌 합치기의 고정 답 공급이 등재된 뒤로 **코드 실재 지점 18쌍 == 등재 18쌍**이다(미등재 0 · 고아 0). 「활성 env」 열은 그 행의 더블을 켜는 배선이며, 배선 자체는 등재 대상이 아니다.

| # | 파일 | 토큰 | CODE | 활성 env | 지점 내용 |
|---|------|------|------|----------|-----------|
| 1 | `backend/src/auth.rs` | `Mode::Stub` | EXT-01 | `FEATUREDOC_DOUBLE_GITHUB_AUTH` | 로그인 authorize가 코드 콜백 리다이렉트로 바로 답하는 분기 |
| 2 | `backend/src/github_api.rs` | `Mode::Stub` | EXT-01 | `FEATUREDOC_DOUBLE_GITHUB_AUTH` | 코드 교환·GitHub 사용자 조회의 결정적 더블 |
| 3 | `backend/src/github.rs` | `Mode::Stub` | EXT-02 | `FEATUREDOC_DOUBLE_GITHUB_APP` | App 설치 유입 URL 대체(설치 완료 콜백 재현) |
| 4 | `backend/src/github_app.rs` | `Mode::Stub` | EXT-02 | `FEATUREDOC_DOUBLE_GITHUB_APP` | 설치 토큰 발급 · 설치 메타 · 접근 가능 저장소 목록 · 저장소 개수 (4곳) |
| 5 | `backend/src/repo_scan.rs` | `Mode::Stub` | EXT-03 | `FEATUREDOC_DOUBLE_REPO_SCAN` | 저장소 트리 스캔과 핵심 파일 본문 읽기(2단계 문맥)의 결정적 더블 (2곳) |
| 6 | `backend/src/llm.rs` | `Mode::Stub` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | `ask`의 stub 응답 디스패치(워커 런타임 경로) |
| 7 | `backend/src/cross_cutting.rs` | `stub: stub_answer` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | 횡단 추출 Ask의 고정 답 공급(제품 경로) |
| 8 | `backend/src/cross_cutting.rs` | `Mode::Stub` | LLM-01 | —(테스트가 직접 지정) | `#[cfg(test)]` 단위 테스트 모듈 내 사용 (3곳) |
| 9 | `backend/src/discovery_strategy.rs` | `stub: stub_answer` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | 탐색 전략 Ask의 고정 답 공급(제품 경로) |
| 10 | `backend/src/discovery_strategy.rs` | `Mode::Stub` | LLM-01 | —(테스트가 직접 지정) | `#[cfg(test)]` 단위 테스트 모듈 내 사용 (2곳) |
| 11 | `backend/src/feature_candidates.rs` | `stub: stub_answer` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | feature 후보 Ask의 고정 답 공급(제품 경로) |
| 12 | `backend/src/llmkey.rs` | `Mode::Stub` | EXT-04 | `FEATUREDOC_DOUBLE_LLM_KEY` | LLM 키 라이브 검증의 형식 기반 더블 |
| 13 | `backend/src/acceptance.rs` | `stub: stub_logic` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | 인수 기준 로직 패스(AC2.1) Ask의 고정 답 공급(제품 경로) |
| 14 | `backend/src/acceptance.rs` | `stub: stub_tests` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | 인수 기준 테스트 패스(AC2.2) Ask의 고정 답 공급(제품 경로) |
| 15 | `backend/src/dependencies.rs` | `stub: stub_dependencies` | LLM-01 | `FEATUREDOC_DOUBLE_LLM` | 종단 의존성 추출(AC2.4) Ask의 고정 답 공급(제품 경로) |
| 16 | `backend/src/doc_edit.rs` | `stub: stub_edit` | LLM-01 | `FEATUREDOC_DOUBLE_LLM`(API) | 편집 제안(AC3.1) Ask의 고정 답 공급(제품 경로 — 워커가 아니라 API 프로세스가 직접 부르는 LLM 지점) |
| 17 | `backend/src/feature_add.rs` | `stub: stub_draft` | LLM-01 | `FEATUREDOC_DOUBLE_LLM`(API) | 빠진 feature 직접 추가(AC3.2)의 근거 찾기 Ask의 고정 답 공급(제품 경로 — 16행과 같이 큐를 타지 않고 API 프로세스가 직접 부른다; 답은 요청 낱말이 든 트리 안 경로에서만 파생) |
| 18 | `backend/src/doc_conflict.rs` | `stub: stub_merge` | LLM-01 | `FEATUREDOC_DOUBLE_LLM`(API) | 자동 결과와 사용자 편집의 충돌 합치기(AC3.5) Ask의 고정 답 공급(제품 경로 — 16·17행과 같이 API 프로세스가 직접 부른다; 답은 사람의 마지막 문장을 그대로 두고 이번 자동 결과를 뒤에 잇되, 이미 버린 문면은 회피한다) |

### 등재 사유

| CODE | 카테고리 | 실환경 불가 사유 | 근거 시나리오 |
|------|----------|------------------|---------------|
| EXT-01 | EXT | 실제 GitHub OAuth 동의 화면·실제 계정 왕복이 필요 — kind CI에서 안전하게 구동 불가 | sc04-11, sc04-12 |
| EXT-02 | EXT | 실제 GitHub App 설치(계정·동의)와 App JWT 서명용 실제 개인키가 필요 | sc04-01, sc01-01 |
| EXT-03 | EXT | 트리 스캔은 실 설치 토큰으로 실 저장소의 recursive git tree를 읽어야 한다. 2단계가 읽는 진입점·매니페스트 파일의 앞부분(`repo_scan::read_files`)도 같은 토큰으로 실 저장소의 contents API를 불러야 해서 같은 경계·같은 env에 속한다. 더블 안의 리비전 스위치 `FEATUREDOC_STUB_REPO_REVISION`(비어 있지 않으면 2 · `3` 이면 3, 더하기만)은 시간이 지나 달라진 트리를 결정적으로 재현한다 — 2 는 문장을 바꾸지 않고 3 은 인수 기준 첫 문장을 다르게 읽게 한다(`acceptance::stub_logic`) | sc01-01, sc01-06, sc02-08, sc03-07 |
| EXT-04 | EXT | 키 라이브 검증은 실 프로바이더 키(과금 자격)로 인증된 요청을 해야 한다 | sc04-03, sc04-04, sc04-13 |
| LLM-01 | LLM | 실 LLM 응답은 비결정적·과금 — 시나리오 3의 결정성 조항(재분석 시 `unchanged`, 항목이 분석 트리의 경로를 근거로 듦)이 고정 답을 요구. AC2.x는 한 걸음 더 나아가 **인수 기준 문장마다 근거 코드 위치가 분석 트리 안에 있는지**를 단정하므로, 근거가 결정적이지 않으면 단정 자체가 성립하지 않는다. AC2.4·AC2.5의 의존성 항목도 같다 — 분류가 7종 안에서 갈리고 근거가 이 분석이 본 경로이거나 명시적으로 비어 있어야 한다는 단정은 고정 답 위에서만 선다. AC3.1의 편집 제안도 같다 — 승인 뒤 시나리오가 **하나 늘고 원래는 남는다**(03#시나리오 1), 두 번째 제안의 문면이 **거부한 것과 다르다**(03#시나리오 2)는 제안 내용에 대한 단정이라 고정 답(요청에 따라 두 모양 중 하나로 답하고, 거부된 문면을 회피하는 규칙을 스텁도 지킨다) 없이는 성립하지 않는다. AC3.2의 근거 찾기도 같다 — 초안의 **모든 근거 경로가 이 분석의 트리 안이고 문장이 가리킨 곳**이며 시나리오 수가 근거 수와 같고 의존성 후보가 7종 안에서 분류된다(03#시나리오 3), 트리에 없는 문장에는 **근거 없음**으로 답해 초안 0건·`evidenceFound=false`가 된다(03#시나리오 4)는 산출물 내용에 대한 단정이라 고정 답(요청 낱말이 든 트리 안 경로만 근거로 드는 결정적 규칙) 없이는 성립하지 않는다 AC3.5 의 합치기도 같다 — 제안이 **내가 보탠 내용과 이번 자동 결과를 모두 담고**(03#시나리오 7), 버린 합친 문장이 다시 오지 않는다는 제안 내용에 대한 단정이라 고정 답(사람의 마지막 문장 + 자동 결과, 거부 문면 회피) 없이는 성립하지 않는다 | sc01-01, sc01-03, sc01-04, sc01-07, sc02-01, sc02-02, sc02-04, sc02-05, sc02-06, sc02-07, sc03-01, sc03-02, sc03-03, sc03-04, sc03-07 |

**단위 테스트 쌍(8·10행)에 관하여**: `#[cfg(test)]` 모듈의 `Mode::Stub` 사용은 E2E 예외가 아니라(cargo test의 모킹은 정상) 지문-등재 1:1 유지를 위해 같이 등재한다. 주석은 각 테스트 모듈 선언 위 1건으로 모듈 안 사용을 덮는다.

## stub mode 제거 — 완료 기록 (2026-09-18, `rct_20260918-0002`)

전역 `FEATUREDOC_MODE` 스위치와 그 파싱·오버레이 패치 **5쌍은 제거됐다.** 이 절은 그것이 어떻게
사라졌는지를 남기는 기록이며, 더 이상 미등재 drift를 기술하지 않는다(**2026-09-18 시점**의 불변식 1:
코드 14쌍 == 등재 14쌍 — 스위치 5쌍이 빠지고 #43이 들여온 인수 기준 단계의 고정 답 2쌍이 등재로 들어온
결과다. 현재 수치는 위 허용목록 절이 갖는다 — 2026-09-19 종단 의존성 추출 1쌍, 2026-09-21 AC3.1 편집 제안 1쌍·AC3.2 빠진 feature 추가 1쌍·AC3.5 충돌 합치기 1쌍이 더해져 18쌍이다).

사용자 판정(2026-09-18 검토 거부 원문 — 「stub mode는 drift 해소 대상」)에 따라 예외 유형으로 굳히지
않고 제거했고, 앞선 문면이 후속 task에 위임했던 **「더블별 활성 방식의 구체 설계」**는 다음과 같이
판정됐다 — **경계마다 전용 env 하나를, 그 경계를 소유한 프로세스만 읽는다**(표는 「모킹으로 세는 것」 1번).

| 제거된 지점 | 무엇으로 대체됐나 |
|-------------|-------------------|
| `backend/src/config.rs` · `FEATUREDOC_MODE` | `Doubles::from_env()` — API가 소유한 세 경계를 각자의 env에서 읽는다 |
| `backend/src/config.rs` · `Mode::Stub` | 스위치 파싱이 사라져 남지 않는다. 값 해석은 `Mode::from_env`가 타입 자신의 생성자로 수행한다 |
| `backend/src/bin/worker.rs` · `FEATUREDOC_MODE` | `WorkerDoubles` — 워커가 소유한 두 경계를 각자의 env에서 읽는다 |
| `backend/src/bin/worker.rs` · `Mode::Stub` | env 파싱 매핑 소멸 + `provider_for`의 키 생략 허용은 **충실도 결함으로 제거**(위 「충실도 보증」 4번) |
| `deploy/e2e/kustomization.yaml` · `FEATUREDOC_MODE` | API 패치가 EXT-01·02·04 env를, 워커 패치가 EXT-03·LLM-01 env를 켠다(2026-09-18 시점. 이후 API 패치도 LLM-01(#92)·EXT-03(#107) env를 켜게 됐다 — 현행은 「모킹으로 세는 것」 1번 표) |

운영 매니페스트(`deploy/k8s/`)의 `FEATUREDOC_MODE: "real"` 두 줄도 같이 지웠다 — 더블이 opt-in이 되어
아무 말도 하지 않는 배포가 곧 전 경계 실연동이고, 파서가 없어진 뒤로는 죽은 설정이기 때문이다.

### 지문 사각지대 고지 (모델 소유자에게)

새 env 이름 `FEATUREDOC_DOUBLE_*`는 이 모델의 현행 as-is 지문 패턴(`Mode::Stub` · `stub: stub_*` ·
**`FEATUREDOC_MODE` 리터럴** · 표기 주석)에 **걸리지 않는다.** 즉 이번 변경으로 배선이 실제로 좁아진
것과, 배선이 지문에서 보이지 않게 된 것은 **다른 사실이며 섞으면 안 된다.** 불변식 1이 닫힌 근거는
「스위치가 사라져서」이지 「스위치가 안 보여서」가 아니지만, 앞으로 새 `FEATUREDOC_DOUBLE_*`가 추가돼도
지문은 침묵한다. 패턴을 `FEATUREDOC_(MODE|DOUBLE_[A-Z_]+)`로 넓히는 것은 **모델 정의(가변부) 개정이라
reconciler-tobe-modeler 소관**이므로 이 task가 처리하지 않고 여기 남긴다. 그때까지의 안전망은 표기 규약과
이 절이다.

**두 번째 사례 (2026-09-19, `rct_20260919-0001`) — 안전망이 실제로 값을 했다.** #71이 들인 종단 의존성
추출의 고정 답 공급자는 이름이 `stub_*`가 아니라 **`stub`**이어서 지문 패턴 `stub: stub_*`에 걸리지
않았다. 이번에 지문이 움직인 것은 오직 **표기 주석** 덕이고(주석마저 없었다면 코드·문서 두 축이 함께
침묵해 미등재가 영구 불가시였다), 그래서 등재와 같은 커밋에서 **`stub` → `stub_dependencies` 개명**으로
이 지점의 사각지대를 닫았다 — 레포 안에서 닫히는 가장 싼 해소이고, 위 「모킹으로 세는 것」 2번이 선언한
`stub_*` 규약을 예외 없이 참으로 되돌린다. 일반 해소(패턴을 `stub: *stub[a-z_]*`로 넓히는 것)는 여전히
모델 정의 개정이라 모델러 소관이며, **같은 자리의 두 번째 사례**라는 사실을 여기 남긴다.

## 차단 요인 원장

**차단 요인** = 치환이 아예 없거나 있어도 표현력이 모자라서 E2E가 그 경로를 **아예 밟지 못하는** 원인. 모킹 지점이 아니므로 코드에 토큰을 남기지 않는다. 개수 자체는 gap이 아니다 — 재는 것은 **해소 계획의 존재**다.

- **B1 인계 수신**: 이 모델을 소관으로 지목한 문장(reconciler 다른 모델의 task 문맥)마다 행이 있어야 한다. **2026-09-22 재실측**(reconciler 전 task 레코드에서 모델 id `tbm_feature-doc-e2e-mock-policy` **정확 일치** sweep — 15 레코드 28 언급. 부분 문자열 `e2e-mock-policy` 로 세면 다른 레포의 같은 이름 모델까지 260 레코드가 걸리므로 반드시 전체 id 로 센다): 28 중 대부분은 「그 파일은 저 모델의 축이니 이 task 가 건드리지 않는다」는 **경계 선언**이라 인계가 아니고, 허용목록 새 쌍의 **등재 위임**(예: `tbm_feature-doc-docs-impl` `rct_20260921-0007`)은 인계이긴 하나 **불변식 1 축에서 닫힌다**(해당 쌍은 이미 등재됨 — 아래 변동 이력). 남는 **진짜 미수신 인계는 1건**이었다 — `tbm_feature-doc-journey-mockup` `rct_20260922-0001` 이 넘긴 `llm::tests` 의 env 레이스. 아래 가림 기록으로 판정해 닫았다(`rct_20260922-0001`) ⇒ **현재 B1 위반 0**.
  **세는 법**(다음 감지가 이 sweep 을 재현할 수 있게 남긴다): 「소관·몫·축·판정 대상·닫지 않는다」류 술어가 **이 모델을 향하고**, 그 대상이 **다른 축에서 이미 닫히지 않은 것**만 센다. 「0건」을 근거 없이 되풀이하지 말 것 — 2026-09-17 자 「지목 문맥 0건」 문장이 살아 있던 동안 위 1건이 원장 어디에도 없이 불가시였다.
- **B2 계획 완비**: 각 행은 해소 방향 · 소관 · 선행(없으면 「없음」 — 그 행은 착수 가능) · 재검토 시점(날짜는 등재일로부터 최장 90일)을 행 안에서 기계적으로 읽을 수 있게 갖춘다.
- **B3 개수 변화**: 증가면 새 행에 B1·B2 적용, 감소면 해소 방향이 실제 반영됐는지 확인, 불변이면 재검토 시점 도과만 본다.

| 행 | 차단 요인 | 해소 방향 | 소관 | 선행 | 재검토 시점 |
|----|-----------|-----------|------|------|-------------|
| (현재 열린 행 없음 — 마지막 해소: R1, 2026-09-18, 위 충실도 보증과 아래 변동 이력 참고. 새 차단 요인은 이 표에 행으로 등재한다.) |

**가림 기록 (차단 요인이 아닌 것)** — doc-tracker 「자동화 밖 잔여」 칸에서 온 후보의 판정 이력:

- **시나리오 1(04) 설치 토큰의 단기 만료** — 차단 요인 **아님**: 설치 토큰은 사용 시점마다 발급되고 저장되지 않아(`backend/src/github_app.rs`의 mint-on-demand, AC4.1) 만료·갱신을 처리하는 제품 분기 자체가 없다. 실 상류 왕복의 뒷면이며 EXT-02의 카테고리 사유로 설명된다.
- **시나리오 2 실제 GitHub 저장소 fetch·브랜치 해석, 시나리오 3·7 LLM 산출물의 의미적 품질** — 차단 요인 **아님**: 등재된 치환(EXT-02·EXT-03·LLM-01)의 카테고리 사유로 설명되는 실 상류 왕복·산출 품질이다. LLM 산출물의 의미적 품질은 이 정책의 대상이 아니다.
- **구현 대기 20건** — 차단 요인 **아님**: 미구현이며 `tbm_feature-doc-docs-impl`의 부채다.
- **AC3.1 편집 제안의 「모델이 고쳐 쓴 문장을 읽을 수 없었습니다」 분기**(`backend/src/doc_edit.rs`의 `proposed` 파서가 답에서 시나리오를 하나도 세우지 못한 경우, 2026-09-21 `rct_20260921-0001` 판정) — 차단 요인 **아님**: `stub_edit`은 항상 스키마 적합 답을 내 E2E가 이 분기를 밟지 못하지만, 03#시나리오 1~8 중 어느 것도 이 실패 경로를 요구하지 않는다(R1이 원장 행이었던 것은 시나리오 6이 LLM 실패를 사전 조건으로 요구했기 때문이다). 파서 경로는 단위 테스트(`an_empty_answer_is_not_a_proposal`)가 지킨다. 스텁의 실 LLM 실패(한도 초과) 표현은 `llm::ask`의 공통 경로(`FEATUREDOC_STUB_LLM_FAIL`)가 이 지점에도 그대로 적용된다.
- **AC3.5 충돌 합치기의 「모델이 합친 문장을 읽을 수 없었습니다」 분기**(`backend/src/doc_conflict.rs`가 `doc_edit::proposed` 파서로 답에서 시나리오를 하나도 세우지 못한 경우, 2026-09-21 슬라이스 6d) — 차단 요인 **아님**: `stub_merge`는 항상 스키마 적합 답을 내 E2E가 이 분기를 밟지 못하지만, 03#시나리오 7 이 요구하는 것은 통합 제안과 승인 요구이지 파서 실패가 아니다. 파서 경로는 `doc_edit` 의 단위 테스트가 지키고 `FEATUREDOC_STUB_LLM_FAIL`도 `llm::ask` 공통 경로라 이 지점에 그대로 적용된다.
- **AC3.2 빠진 feature 추가의 「트리 밖 근거 폐기」 분기**(`backend/src/feature_add.rs`의 `grounded`가 답의 근거 경로 중 이 분석이 본 트리에 없는 것을 버리는 경우, 2026-09-21 `rct_20260921-0003` 판정) — 차단 요인 **아님**: `stub_draft`는 요청 낱말이 든 트리 안 경로만 근거로 들어 E2E가 이 분기를 밟지 못하지만, 03#시나리오 3·4가 요구하는 것은 「근거가 트리 밖으로 나가지 않는다」는 규칙과 「없으면 지어내지 않는다」이지 폐기 분기 자체가 아니다. 폐기 규칙은 stub 답에도 같은 코드(`grounded`)가 적용되고 단위 테스트(`an_invented_evidence_path_is_dropped` · `a_claim_without_grounded_scenarios_is_not_found`)가 지킨다. `FEATUREDOC_STUB_LLM_FAIL`도 `llm::ask` 공통 경로라 이 지점에 그대로 적용된다.
- **`llm::tests` 의 `FEATUREDOC_STUB_LLM_FAIL` 프로세스 전역 레이스**(`backend/src/llm.rs` — `stub_llm_fail_trigger_fires_only_on_the_named_input` 이 set 하던 needle `"a\nb"` 를 형제 `stub_is_deterministic_for_the_same_ask` 가 쓰는 공유 픽스처 `an_ask()` 의 user `"a\nb\nc"` 가 포함해 `cargo test` 가 간헐적으로 429 로 깨지던 것. **인계원**: `tbm_feature-doc-journey-mockup` `rct_20260922-0001`, 2026-09-22 `rct_20260922-0001` 판정) — 차단 요인 **아님**: E2E 는 이 경로를 **실제로 밟는다** (`e2e/tests/sc01-06-partial-retry.spec.ts` 가 워커 env 로 이 트리거를 직접 켜 시나리오 6 의 사전 조건인 LLM 한도 초과 실패를 재현한다). 막힌 것이 아니라 **cargo 단위 테스트의 격리 결함**이므로 원장 행이 아니다. **코드 수정 소관은 이 모델**이다 — 인계원이 `tbm_feature-doc-docs-impl` 과 이 모델을 **나란히 적은 선택지형 지목**을 남겼으나, 문제의 트리거와 그 안전성을 주장하던 주석은 **이 모델이 원장 R1 해소로 직접 착지시킨 산출물**이고(변동 이력 2026-09-18 `rct_20260918-0001` 행), 주석 자신이 `blocker-ledger R1, docs/e2e-mocking-policy.md` 를 근거로 인용한다. 자기가 들인 자리의 거짓 충실도 주장을 고치는 것은 문서↔구현 축이 아니라 이 축이다. **해소는 같은 판정 PR 에서 동승**했다 — 트리거 테스트의 needle 을 다른 테스트의 프롬프트가 담을 수 없는 고유 문자열로 바꾸고, 주석의 「no other test reads this variable, so parallel test runs cannot race on it」 **두 절이 거짓임을 정정**했다(`ask` 가 부르는 `stub_answer` 가 이 바이너리의 모든 Stub-mode ask 에서 그 env 를 읽는다). 「The env writes stay inside this one test」 절은 실측상 참이라 남겼다(쓰기는 그 테스트 안 3곳뿐).

## 변동 이력

| 날짜 | task | 내용 |
|------|------|------|
| 2026-09-17 | rct_20260917-0001 | 문서 신설: 정책·허용목록 17쌍(EXT 4종·LLM 1종·스위치 유형)·원장 R1·가림 기록·충실도 보증 2건 확정 |
| 2026-09-18 | rct_20260917-0001 | 검토 반영 재작업: SW-01 유형 등재 철회(사용자 지시 — stub mode는 drift 해소 대상), 허용목록 17→12쌍, 스위치 지점 주석 제거, stub mode 제거 계획 표 신설, 허브 등록 |
| 2026-09-18 | rct_20260918-0002 | 전역 `FEATUREDOC_MODE` 스위치 제거(미등재 5쌍 소멸): 경계별 `FEATUREDOC_DOUBLE_*` 활성 배선 도입, `provider_for`의 키 생략 허용을 충실도 결함으로 제거(+ `sc04-07`·`sc04-08` 키 셋업 보강), 허용목록에 「활성 env」 열 추가, 제거 계획 표를 완료 기록으로 전환, 지문 사각지대 고지 신설 |
| 2026-09-18 | rct_20260918-0002 | 위 재계획 중 main에 착지한 #43(AC2.1~2.3 인수 기준 단계)이 `backend/src/acceptance.rs`에 고정 답 공급 2쌍을 **미등재·무주석으로** 들여왔다. 기존 LLM 단계 3쌍(7·9·11행)과 같은 경계·같은 메커니즘이므로 LLM-01로 등재하고 표기 주석을 달았다 — 허용목록 12→14쌍(10→11파일). 불변식 1은 이 등재까지 포함해야 닫힌다 |
| 2026-09-19 | rct_20260919-0001 | #71(슬라이스 5b-1)이 `backend/src/dependencies.rs`에 들인 종단 의존성 추출(AC2.4) Ask의 고정 답 공급 1쌍을 LLM-01로 등재 — 허용목록 14→**15쌍**(11→12파일), LLM-01 근거 시나리오에 `sc02-05`·`sc02-06`·`sc02-07` 추가. #71은 표기 주석은 규약대로 달았고 등재만 빠뜨렸다. 함께: 공급자 이름이 `stub_*` 규약을 벗어나(`stub`) as-is 지문에 걸리지 않던 것을 **`stub_dependencies` 개명**으로 복원 — 이번 감지는 표기 주석이라는 안전망만으로 서 있었다(위 「지문 사각지대 고지」의 두 번째 사례) |
| 2026-09-21 | rct_20260921-0001 | #92(슬라이스 6a)가 `backend/src/doc_edit.rs`에 들인 AC3.1 편집 제안 Ask의 고정 답 공급 1쌍(`stub: stub_edit`)을 LLM-01로 등재 — 허용목록 15→**16쌍**(12→13파일), LLM-01 근거 시나리오에 `sc03-01`·`sc03-02` 추가. #92는 표기 주석을 규약대로 달았고 공급자 이름도 `stub_*` 규약을 지켜 지문이 주석·토큰 두 축으로 잡혔다(개명 불요) — 등재만 빠뜨렸다(#71에 이어 docs-impl 슬라이스의 **같은 형태 두 번째**). 함께: 이 지점은 워커가 아니라 **API 프로세스**가 `FEATUREDOC_DOUBLE_LLM`을 읽는 첫 LLM-01 쌍이라 배선 표의 「읽는 프로세스」와 「각 env는 그 경계를 소유한 프로세스만 읽는다」 문단을 #92 이후 사실로 정정. 파서 실패 분기는 가림 기록으로 판정(원장 행 신설 없음) |
| 2026-09-21 | rct_20260921-0003 | #107(슬라이스 6b)이 `backend/src/feature_add.rs`에 들인 AC3.2 빠진 feature 직접 추가의 근거 찾기 Ask 고정 답 공급 1쌍(`stub: stub_draft`)을 LLM-01로 등재 — 허용목록 16→**17쌍**(13→14파일), LLM-01 근거 시나리오에 `sc03-03`·`sc03-04` 추가. #107은 표기 주석·`stub_*` 이름 규약을 지켰고 등재만 빠뜨렸다(#71·#92에 이어 docs-impl 슬라이스의 **같은 형태 세 번째**). 함께: #107로 API 프로세스도 `FEATUREDOC_DOUBLE_REPO_SCAN`을 읽게 됐으므로(`config.rs` `Doubles.repo_scan` · 오버레이 API 패치 · `feature_add.rs`의 트리 재스캔 — 새 지점이 아니라 5행 `repo_scan.rs`를 두 프로세스가 부른다) 배선 표 EXT-03 「읽는 프로세스」·소유 문단·완료 기록 표 문면을 #107 이후 사실로 정정. `grounded`의 트리 밖 근거 폐기 분기는 가림 기록으로 판정(원장 행 신설 없음) |
| 2026-09-21 | rct_20260921-0012 | 슬라이스 6d(AC3.5)가 `backend/src/doc_conflict.rs`에 들인 충돌 합치기 Ask 고정 답 공급 1쌍(`stub: stub_merge`)을 **같은 PR 에서** LLM-01로 등재 — 허용목록 17→**18쌍**(14→15파일), LLM-01 근거 시나리오에 `sc03-07` 추가(#71·#92·#107 이 세 번 반복한 「등재 누락」을 이번엔 슬라이스가 직접 닫았다). 함께: EXT-03 의 리비전 스위치가 값을 읽게 됐다(`FEATUREDOC_STUB_REPO_REVISION` — 비어 있지 않으면 2, `3` 이면 3, 더하기만) — 3 은 인수 기준 첫 문장을 다르게 읽는 코드 변경의 결정적 재현이라 EXT-03 근거 시나리오에 `sc02-08`·`sc03-07` 을 적었다. 파서 실패 분기는 가림 기록으로 판정(원장 행 신설 없음) |
| 2026-09-18 | rct_20260918-0001 | 원장 R1 해소(충실도 확장): `llm::stub_answer`에 결정적 실패 트리거 착지(`FEATUREDOC_STUB_LLM_FAIL` — 프롬프트 부분열 일치 시 real과 같은 `LLM rejected the request (429)`), sc01-06이 시나리오 6 원문의 사전 조건(LLM 한도 초과로 실패한 feature 추출 단계)을 직접 재현. 트리 404 우회 arc는 기존 단정으로 유지하고 doc-tracker 비고를 갱신 |
| 2026-09-22 | rct_20260922-0001 | **B1(인계 수신) 재실측 + 미수신 인계 1건 수신·판정**: `tbm_feature-doc-journey-mockup` `rct_20260922-0001` 이 넘긴 `llm::tests` env 레이스를 가림 기록 1행으로 판정(차단 요인 아님 — E2E 는 `sc01-06` 으로 이 경로를 밟는다)하고, **선택지형 지목의 소관을 이 모델로 확정**(트리거가 이 모델의 R1 해소 산출물). B1 절의 「지목 문맥 0건(2026-09-17 전수 확인)」이 **거짓이 되어 다음 감지를 재굴착 없이 닫게 하던 것**을 재실측값(정확 id sweep 15 레코드 28 언급, 실 미수신 1건)과 **세는 법**으로 교체. 함께: 레이스 해소를 같은 PR 에 동승 — 트리거 테스트 needle 비중첩화 + `llm.rs` 주석의 거짓 2절 정정. 경미: 「stub mode 제거」 절 괄호의 열거 합 17→**18쌍**(2026-09-21 AC3.5 1쌍 누락분 반영 — 실제 수치는 그때 이미 18). 허용목록 표·등재 사유 표·표기 주석 무접촉(불변식 1 은 코드 18쌍 == 등재 18쌍으로 닫혀 있다) |
