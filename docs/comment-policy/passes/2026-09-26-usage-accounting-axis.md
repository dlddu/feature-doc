# 비용 집계 축 신설 3파일 — 슬라이스 7d 가 들여온 AC4.6 적재·집계·검증 (37차 패스)

- task: `rct_20260926-0002` · 모델 `tbm_feature-doc-comment-redundancy`
- 기준 커밋: `0522f33`(#172 착지 직후의 main tip) · 부모 지문 `lines=2968 files=150`
- 원장 행: **신설 1행**(행 34) — `backend/src/usage.rs` · `backend/tests/usage.rs`
- **판정 96행**(54+19+23) / **집행 73행** — `sc04-09` 23행은 판정만 하고 **집행을 보류**했다(아래 「보류」 절)
- 순 제거 **47행**(73 → 26) · 유지 **26행** · 보류한 파일의 판정은 **순 제거 19행 · 유지 4행**(미집행)

## 이 세 파일이 왜 한 행인가

슬라이스 7d(PR **#166**, 자매 모델 `tbm_feature-doc-docs-impl` `rct_20260925-0011`)가
AC4.6(관측 가능성·비용 가시성)을 닫으면서 들여온 **신설 파일 전부**다. 집계(`usage.rs`) ·
그 계약의 통합 테스트(`tests/usage.rs`) · 시나리오 e2e(`sc04-09`)가 **한 명제 집합의 세 면**이라,
같은 문장이 세 파일에 번역·환언으로 흩어져 있다. 행을 갈라 판정하면 「정본 지정」이 행 경계를 넘어
서로를 인용하게 되므로 한 행으로 묶었다.

같은 슬라이스가 들여온 **`backend/migrations/0015_llm_call_usage.sql` 11행은 이 행에 넣지 않았다** —
D1(사람 게이트) 몫이라 아래 「범위 밖」 절에 귀속해 둔다.

## 🔴 보류 — `sc04-09` 는 판정만 하고 집행하지 않는다

판정을 마치고 PR 을 열기 직전에 자매 PR **#174**(슬라이스 7e — AC4.6 누적 호출·토큰의 **화면 표시**,
모델 `tbm_feature-doc-docs-impl` `rct_20260926-0002`)가 열려 있는 것을 다시 재어 확인했다. 그 PR 은
이 패스가 **제거하려는 머리 ¶2 를 고쳐 쓰고**(「운영자 화면」 절을 쪼개 「사용자 쪽은 같은 이유로
닫히지 않는다 — 시나리오 9 의 동사는 「접근 가능」이 아니라 「확인」·「표시된다」」를 덧붙인다), 이 패스가
지우려는 **절 제목 주석 셋을 자기 헝크의 문맥 줄로 쓴다**. `git merge-base` 로 계산한
`git merge-tree` 실측은 **이 파일 하나만** `CONFLICT (content)` 이고 나머지 11파일은 clean 이다.

그래서 **겹치는 헝크만 보류한다** — `sc04-09` 를 부모(`0522f33`) 상태로 되돌려 원장 행 34 의 범위에서
빼고, 23행을 ⑴(행 열 밖 잔여)에 남긴다. 착지 순서가 자유로워지고, 아래 판정 표는 그대로 남아
다음 주기가 **모집단이 아니라 판정을 승계**한다(#174 의 유입분만 더해 재판정하면 된다).

⚠️ #174 는 `frontend/src/HomeRepositories.tsx`·`format.ts`(원장 행 10)와 `AnalysisProgress.tsx`(행 7)도
건드린다. 이 패스의 행 10 편집은 `api.ts` 한 파일이라 **텍스트 충돌은 없지만**, #174 가 먼저 착지하면
행 10 의 줄 수·지문과 합계 절의 세 값을 **리베이스 뒤 다시 재야 한다**(나머지 여섯 행은 #174 의 파일
집합과 겹치지 않아 base 이동에 불변이다).

## 판정 규칙 — 33차 패스가 세운 세 갈래를 그대로 쓴다

#166 도 reconciler 루프가 만든 PR 이라 본문(4,788자)이 설계 판단을 **절 단위로** 적는다. 그래서
거의 모든 줄이 **경로 ③** 히트가 되고, 규칙 없이 적용하면 유지가 0 에 수렴한다(원장 행 14 증분 ②
가 먼저 세운 문제). 갈래는 그대로다:

- **닫는다(서사)** — 「왜 이렇게 만들었나」 · 「무엇이 비어 있었나」 · AC 꼬리표 · 문서 이름 지목 ·
  절 제목 · 단계 열거 · 선언·시그니처·테스트 이름 재진술. 테스트 **함수 이름**은 강력한 ① 원본이다.
- **남긴다(가드)** — 「이 값이 무엇과 같아야 하는가 / 무엇을 넣지 말라 / 이걸 빼면 무엇이 **조용히**
  약해지는가」. 판별식은 **어겼을 때 붉어지는가**다 — 이름 붙은 단위 테스트가 그 명제를 집행하면
  그것은 조용하지 않으므로 서사 쪽으로 넘어간다.
- **정본 지정** — 같은 명제가 여러 벌이면 **어길 사람이 읽는 자리** 한 벌만 남긴다.

### 이 축에서 정본을 옮긴 두 명제

| 명제 | 사본 | 정본 | 근거 |
|---|---|---|---|
| 「한 행이 두 호출을 대표할 수 있다 — 행을 세면 하나를 잃는다」 | `usage.rs` `CALL_ROWS` doc · `acceptance.rs` 인라인 3 · `tests/usage.rs` `submit_doc` doc·테스트 doc · `llm.rs` `Answer` doc · `0015` 주석(범위 밖) | **`llm.rs` 의 `Answer.calls` doc** | 그 필드를 상수로 바꾸려는 사람이 읽는 자리다. `acceptance::derive` 의 합산식(①)과 `a_merged_stage_row_counts_both_of_its_calls`(①)가 나머지를 복원한다 |
| 「운영자 면은 세우지 않고 `/api/usage` 한 주소로 닫았다 — 두 번째 사본은 어긋날 수 있는 두 번째 자리다」 | `usage.rs` `UsageView` doc 6 · `sc04-09` 머리 5 | **`docs/doc-tracker/2026-09.md:128`**(경로 ②, 판정 범위 밖) | 운영자 화면을 새로 지으려는 사람은 여정·시나리오 문서를 읽는다. 원장 문면은 ②·③ 양쪽에 **70자 이상 연속 일치**로 이미 있다 |

## 판정 표 — `backend/src/usage.rs` (54 → 16)

| 자리 | 문면(요약) | 판정 | 근거 |
|---|---|---|---|
| 모듈 doc 머리 1 | `Measured LLM usage and what it cost (AC4.6).` | **유지 1** | `//!` 모듈 머리 요약 1줄(README 「유지 대상」) |
| 모듈 doc ¶2 (6) | 네 테이블(`analysis_documents`·`feature_dependency_requests`·`feature_doc_edits`·`feature_additions`)이 이미 토큰을 적고 있고 「비어 있던 것은 집계와 주소였다」 | **제거 6** | ① `CALL_ROWS` 의 `UNION ALL` 이 네 테이블을 그대로 열거한다 · ③ #166 본문 「적재처는 이미 넷이었다」 표 4행 + 「비어 있던 것은 **적재가 아니라 집계와 주소**였다」 — 「무엇이 비어 있었나」는 경위다 |
| 모듈 doc ¶3 앞 3 | 카운터 열이 아니라 **파생**으로 둔 이유 — 카운터는 모든 쓰기 자리에서 맞아야 하고 한 번 어긋나면 계속 틀리지만, 이미 있는 행의 합은 그 행들과 어긋날 수 없다 | **유지 3** | 「무엇을 넣지 말라」형 가드이고, 어겼을 때(카운터 열 신설) **아무 테스트도 붉어지지 않는다**. ③ 히트(#166 「카운터 열로 두지 않은 것이 설계 판단이다」)가 있어도 **편집 지점에서 읽히지 않는다** |
| 모듈 doc ¶3 뒤 2 | 「그래서 소급해 자기 실비용을 말한다 — 이 슬라이스 이전에 돌아간 분석도」 | **제거 2** | ③ #166 본문 축자(「이 슬라이스 이전에 돌아간 분석도 **소급해 자기 실비용을 말한다**」) · ① 파생 합의 자명한 귀결이고 소급성을 재는 테스트는 없다 · 「이 슬라이스」는 작업 흔적 표기 |
| 모듈 doc ¶4 (5) | **Estimate, not an invoice** — 제공자 단가는 모델·시점마다 다르고, AC4.6 이 요구하는 것은 「추정 비용」이며 pre-flight 추정은 그것이 아니다 | **제거 5** | 같은 파일 안의 **쌍둥이**(단가 상수 doc 이 「stale table reads as fact while a single documented rate reads as the estimate it is」로 같은 명제를 갖는다) → 정본은 단가 상수 쪽 · 「AC4.6 이 요구하는 것」은 ② AC 재진술 · pre-flight 의 정체는 ① `analysis.rs` 의 `Estimate` doc |
| 단가 상수 doc 6 → 4 | 「백만 토큰당 센트」 + 「모델별 표를 두지 않는 이유」 + 「출력이 입력보다 비싸다」 | **제거 2 · 유지 4** | 제거: ① 이름 `INPUT_CENTS_PER_MTOK`(`MTOK`) · ① 테스트 `the_rates_are_applied_per_million_tokens` · ① 테스트 `output_is_priced_above_input`. 유지: 「모델별 단가표를 만들지 말라」는 상수를 고치려는 사람이 읽는 자리의 가드이고 ③(#166 「범위 밖(후속)」)는 그 자리가 아니다 |
| `Spend` doc 4 → 1 | 「`cost_cents` 는 올림이라 최소 1센트 — 호출이 있었는데 `$0.00` 이라 말하지 않기 위해」 | **제거 3 · 유지 1** | ① 테스트 두 개가 이름으로 집행한다(`no_usage_costs_nothing` · `any_usage_costs_at_least_one_cent`) ⇒ 어기면 **즉시 붉다** · ③ #166 본문 축자. 요약 1줄만 남긴다 |
| `cost_cents` fn doc 1 | `Ceiling division of the rate product by a million — see [`Spend`].` | **제거 1** | ① 본문 `(micros + 999_999) / 1_000_000` 이 그 문장이다 · rustdoc 링크만을 위한 문장은 README 가 명시로 금지 |
| `CALL_ROWS` doc 6 → 2 | 요약 + 「`analysis_documents` 만 자기 `calls` 를 갖는 이유(마이그레이션 `0015`)」 + 「NULL `model` 은 아직 호출이 안 앉은 행」 | **제거 4 · 유지 2** | 제거: ① SQL 자신이 `calls` 대 리터럴 `1` 의 비대칭을 보이고, 뒤집으면 `a_merged_stage_row_counts_both_of_its_calls` 가 붉어진다 · 정본은 `llm.rs`(위 표). 유지: `WHERE model IS NOT NULL` **을 지우는 사람**이 읽는 자리이고, 그 필터를 빼면 큐에 든 행이 0 토큰 호출로 세어져 **조용히** 어긋난다 |
| `of_analysis` doc 2 | `One analysis's measured spend. Zero for an analysis that has not called anyone yet…` | **제거 2** | ① 시그니처(`of_analysis(db, analysis_id) -> Spend`) 재진술 + `COALESCE(SUM(...), 0)` · ① 테스트 `an_analysis_that_called_no_one_spends_nothing` · 원장 행 12 의 「선언·시그니처만 영어로 풀어 쓴 `///` 요약」 선례 |
| `cost_cents` 필드 doc 2 | 「SQL 에서 합산하지 않는다 — 단가는 `cost_cents` 가 소유해 한 자리에서만 계산된다」 | **유지 2** | SQL 쪽에서 같은 단가로 계산해도 **모든 테스트가 초록**이고 그 순간 단가가 두 벌이 된다 — 조용한 파손 |
| `UsageView` doc 8 | 「작업별·전체별을 한 답에 (test/04 시나리오 9)」 + 운영자 면을 세우지 않은 이유 | **제거 8** | 앞 2: ① 필드 `total`·`analyses` 와 `WHERE a.user_id = ?` · ② 문서 이름 지목(`test/04 시나리오 9`). 뒤 6: ② `doc-tracker/2026-09.md:128` 과 **70자 이상 연속 일치** + ③ #166 「운영자 면은 세우지 않았다」 절 — 정본을 원장으로 옮겼다(위 표) |
| `read()` 인라인 3 → 2 | 「합계는 합친 토큰에서 한 번 계산한다 — 행마다 올림해 더하면 분석마다 올림을 물린다」 + 「전체별은 한 무더기 토큰에 대한 한 숫자다」 | **제거 1 · 유지 2** | 유지: 이 줄을 어겨 `analyses` 의 `cost_cents` 합으로 바꾸면 `summing_tokens_then_costing_is_not_the_same_as_costing_each_row` 는 **여전히 초록**이고(그 테스트는 산술만 잰다) 통합 테스트의 등식도 호출·입력·출력 세 축만 잰다 ⇒ 조용한 파손. 제거: 세 번째 문장은 장식 |
| 단위 테스트 인라인 2 | 「두 분석이 각자 올림하면 둘이 합쳐 2센트다 — 합계는 한 번만 올림한다」 | **제거 2** | ① 테스트 이름 + 바로 아래 세 `assert_eq!` 가 그 문장이다 |

## 판정 표 — `backend/tests/usage.rs` (19 → 10)

| 자리 | 문면(요약) | 판정 | 근거 |
|---|---|---|---|
| 모듈 doc 머리 2 | `Measured LLM usage: what /api/usage and the analysis detail add up, and whose rows they are allowed to add (AC4.6).` | **유지 2** | `//!` 모듈 머리 요약. 자매 통합 테스트 넷(`doc_conflict.rs`·`doc_edit.rs`·`feature_add.rs`·`feature_delete.rs`)이 같은 모양의 `//!` 머리를 판정받고 **유지**한 선례 |
| 모듈 doc ¶2 (5) | 「행을 직접 INSERT 하지 않고 워커의 `/internal` 라우트로 쓰는 이유 — 손으로 지은 행은 워커가 실제로 보내는 것과 갈릴 수 있고, 시험 대상은 바로 그 전송이다」 | **유지 5** | 어기면(행을 직접 INSERT) **테스트는 초록인 채 다른 것을 재게 된다** — 가장 조용한 파손. 문면이 지목하는 원본은 `documents.rs` 의 **주석**이라 복원 경로 넷 밖이다 |
| `submit_doc` doc 2 | 「한 stage 문서를 보낸다. `calls` 는 병합 답변 stage 가 보고하는 값 — `acceptance::derive` 참고」 | **제거 2** | ① 시그니처(`submit_doc(state, id, kind, calls, input, output)`) · 병합 명제의 정본은 `llm.rs` |
| `per_analysis_and_total_spend_add_up` doc 2 | 「작업별과 전체별이 같은 행에서 나오고 서로 맞는다 — 출처가 갈리면 언젠가 어긋난다」 | **제거 2** | ① 테스트 **이름 자체**가 그 문장이고 본문 세 `assert_eq!` 가 등식이다 |
| 인라인 1 | 「출력 토큰이 더 비싸다 — 두 분석은 토큰 수가 같고 방향만 다르다」 | **유지 1** | `assert!(by_id(second) > by_id(first))` 하나만 남으면 **왜 참인지**가 픽스처 대칭성에 있다는 사실이 사라지고, 픽스처를 고치는 순간 단정이 **조용히 공전**한다 |
| `a_merged_stage_row_counts_both_of_its_calls` doc 1 | 「한 행이 두 호출을 대표할 수 있다 — 행을 세면 하나를 잃는다」 | **제거 1** | ① 테스트 이름 · 정본은 `llm.rs` |
| 음성 프로브 인라인 2 | 「같은 행이 1회라고 말하면 합계도 1이어야 한다 — 값이 실제로 읽히고 있다는 뜻이고 2 가 상수로 박힌 것이 아니라는 뜻이다」 | **유지 2** | 음성 프로브의 존재 이유는 「이 단정을 빼면 무엇이 조용히 통과하는가」다. 지우면 다음 사람이 「중복된 두 번째 단정」으로 보고 걷어낸다 · ③ 히트(#166 「음성 프로브로 감도 실측」)가 있어도 편집 지점이 아니다 |
| `a_report_without_a_call_count_still_counts_one_call` doc 1 | 「필드를 모르는(옛) 워커가 보고해도 호출은 0 이 아니라 1 로 센다」 | **제거 1** | ① 테스트 이름 축자 |
| `one_users_spend_never_appears_in_anothers` doc 1 | 「비용 가시성은 본인 것만 본다 — AC4.7 의 격리가 이 화면에도 적용된다」 | **제거 1** | ① 테스트 이름 축자 + AC 꼬리표 |
| `an_analysis_that_called_no_one_spends_nothing` doc 1 | 「호출이 없으면 0 이다 — 빈 값이 아니라 답이다」 | **제거 1** | ① 테스트 이름 축자 · `COALESCE` |
| `usage_is_closed_to_anonymous_requests` doc 1 | 「미인증은 비용을 못 본다」 | **제거 1** | ① 테스트 이름 축자 |

## 판정 표 — `e2e/tests/sc04-09-per-user-cost-visibility.spec.ts` (23 → 4, **집행 보류**)

아래 판정은 확정이고 집행만 #174 뒤로 밀렸다. 다음 주기는 이 표를 승계한다.

| 자리 | 문면(요약) | 판정 | 근거 |
|---|---|---|---|
| 머리 ¶1 (6) | 「이 spec 이 재는 것은 그 숫자가 실제 호출에서 나왔는가다 … 「0 이 아니다」로 단정하면 추정을 실측으로 오인하고도 초록이 된다 … 대신 호출 전후의 델타로 잰다」 | **제거 6** | 같은 파일 안 **쌍둥이**(단정 바로 위의 인라인 2행이 같은 명제의 압축형) → 정본은 인라인 쪽 · ② `doc-tracker:128` 「비용의 **절대값**은 단정하지 않는다 … 대신 **델타와 등식**으로 잰다」 · ③ #166 「**델타로 잰다.** pre-flight 추정은 호출을 한 번도 안 해도 0 이 아니므로…」 축자 |
| 머리 ¶2 (6) | 「운영자 화면에서도 동일한 데이터에 접근 가능」은 같은 주소로 닫는다 + 운영자 역할·여정이 없다 + 단계별 내역은 `llm usage recorded` 로그 | **제거 6** | ② `doc-tracker:128` 과 **70자 이상 연속 일치**(「이 제품에는 운영자 역할도 운영자 여정도 없고(여정 6개가 전부 최종 사용자용…), 같은 숫자의 두 번째 사본은 원본과 어긋날 수 있는 두 번째 자리」 + 「단계별 내역은 `worker_api::submit_document` 의 `llm usage recorded` 로그가 남긴다」) · ③ #166 같은 절 |
| 임대 고지 2 | `//` + `// Leases the analysis worker — lease rules in \`e2e/support/cluster.ts\`.` | **유지 2** | e2e spec **12파일이 공유하는 축약형 규약**이고 이미 판정받아 유지된 문면이다 — 한 축에서 혼자 지우면 첫 이탈이 된다 |
| 음성 대조 인라인 2 | 「호출 전: 측정치는 0 이다. pre-flight 추정은 이미 0 이 아니므로, 이 단정이 바로 『화면이 추정을 실측으로 부르지 않는다』는 음성 대조다」 | **유지 2** | `toBe(0)` 을 「0 초과」로 고치면 **여전히 초록이면서 아무것도 재지 않는다** — 이 축에서 가장 조용한 파손이고, 단정 바로 위가 그 편집 지점이다 |
| 절 제목 인라인 5 (1+1+1+1+1) | 「두 분석을 모두 3단계까지 돌린다」 · 「작업별 — 화면이 이 분석의 측정 비용을 띄운다」 · 「새로고침해도 같다」 · 「델타가 곧 증거다」 · 「후보 화면도 같은 값을 쓴다 — 4단계까지 열어」 | **제거 5** | ④ 유형(바로 아래 코드가 이미 말하는 절 제목). 「새로고침해도 같다」는 `page.reload()` 뒤 같은 단정이, 「델타가 곧 증거다」는 `toBeGreaterThan(before.total.llmCalls)` 가, 나머지는 클릭·폴링 열거가 그 문장이다 · 「델타」 명제의 정본은 위 인라인 2행 |
| 전체별 등식 인라인 2 | 「전체별 — 작업별의 합과 같아야 한다. 두 숫자가 다른 곳에서 나오면 언젠가 어긋나므로 등식으로 잰다」 | **제거 2** | ① 바로 아래 세 `toBe(rows.reduce(...))` 가 **그 등식 자체**다 · ② `doc-tracker:128` 「**전체별 = 작업별 합**(호출·입력·출력 세 축 등식)」 |

## 무영향 증명

- **비주석 diff 0줄** — 이 패스가 건드린 11파일 전체에서 추가·삭제 줄 중 주석 시작 패턴에 걸리지 않는
  것이 하나도 없다(`git diff -U0` 을 `^[+-]\s*(//|#|/\*|\*|--)` 로 필터해 **잔여 0**).
- `cargo test --offline --profile ci` **26개 테스트 바이너리 전부 ok · 0 failed · doc-test 0건** ·
  `tsc -b` rc=0 · `npx playwright test --list` **38파일 40테스트** rc=0.
- **음성 프로브 둘**(내 측정의 감도 실측 — 이 축에는 레포 체커가 0건이라 측정 자신을 재야 한다):
  ⒜ 제거한 주석 2행을 되살리면 **행 34 지문이 26 ↔ 27 로 어긋나고** 합계 절의 등식 문자열도 깨진다.
  ⒝ 새 행 34 를 원장에서 지우면 그 파일들이 「행 열 밖」으로 돌아가 **⑴ 가 같은 값만큼 늘어 항등식은
  그대로 초록**이다(자기 치유) — 잡는 것은 **계산한 등식 문자열이 원장 산술 블록에 그대로 있는가** 쪽이다.
- 파이썬 docstring 같은 **지문 밖 주석은 이 축에 없다** — 세 파일은 Rust·TypeScript 이고,
  JSDoc 닫는 `*/` 는 `frontend/src/api.ts`(원장 행 10, 아래 증분 재판정 ⑭)에서만 움직인다.

## 범위 밖 — 다음 감지 주기의 몫

- 🔴 **`backend/migrations/0015_llm_call_usage.sql` 11행** — 같은 슬라이스 7d 가 들여왔고 **제거 근거가
  이 축에서 가장 강하다**(#166 본문과 **94자 연속 일치**: 「인수 시나리오 단계는 논리 호출과 테스트 호출
  둘을 한 문서로 합쳐 한 행만 남기고(`acceptance::derive`), 테스트 경로가 없는 저장소에서는 한 번만
  부른다」). 그런데 `tools/check-data-format-change.py` 는 `backend/migrations/` 의 **모든** 변경에 D1 을
  발화시키므로(`classify()` 의 `f.startswith("backend/migrations/")`), 주석 한 글자만 지워도 이 PR 전체가
  사람 승인·수동 repair 게이트에 묶인다. README 「적용된 마이그레이션」 2항이 **「다른 주석 정리와 섞지
  않고 별도 PR 하나로」** 를 명시하므로 이 패스에서는 손대지 않는다. 다음 task 의 **전용 PR** 몫이다.
- **원장 행 11 의 +23행**(`tools/check-mockup-render.py`) — PR **#172**(`0522f33`, 목업↔구현 수렴 M9,
  자매 `tbm_feature-doc-mockup-render` `rct_20260926-0001`)이 이 패스의 claim **뒤**(07:32Z)에 착지시켰다.
  이 task 의 감지 baseline(`lines=2945`) 밖이라 판정하지 않고 귀속만 적는다.
- **`e2e/tests/sc04-09-per-user-cost-visibility.spec.ts` 23행** — 위 「보류」 절. 판정은 끝났다.
