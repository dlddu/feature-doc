# 판정 상세 — `tools/` 체커 축 (정적 게이트 3파일)

- **판정일**: 2026-09-20
- **판정 범위**: `tools/check-mockup-render.py` · `tools/check-journey-mockup.py` ·
  `tools/check-scenario-e2e.py`
- **기준 트리**: 판정은 `7a232f9`, 병합 기준은 리베이스 후 부모 **`b2724da`** (main — #64 가
  판정 도중 머지됐다)
- **reconciler task**: `tbm_feature-doc-comment-redundancy/rct_20260920-0003`

규칙은 [../README.md](../README.md), 판정 결과의 표면은 [../ledger.md](../ledger.md)에 있다.
이 파일은 이번 범위의 **근거**만 담는다.

## 범위를 이 3파일로 고른 이유

원장이 직전 패스에서 다음 축을 이미 지목해 뒀다 — 「`tools/` 체커 전량이 한 축으로 잡힌다.
`check-journey-prototype.js` 는 원장 2행이 이미 판정했으므로 그 축을 집으면 `tools/` 가
디렉터리 단위로 닫힌다」. 그 지목대로 집었고, **계획 시점 트리의 판정 대상 4파일은 전량
닫혔다**.

다만 **디렉터리는 닫히지 않았다** — 판정하는 동안 #64 가 머지돼 `tools/check-data-format-change.py`
(주석 58행)가 다섯 번째 파일로 들어왔다. 파일 겹침이 0 이라 이 패스의 판정·지문에는 영향이
없지만, 「`tools/` 종료」라고 적어 두면 다음 패스가 그 디렉터리를 건너뛰어 그 58행을 놓친다.
원장의 「경합」 절에 그렇게 적었다.

세 파일을 한 패스로 묶은 이유는 **서로가 서로의 원본**이기 때문이다. 셋은 같은 방침으로
쓰인 형제 게이트라 같은 명제(의존성 0 · 등재가 유일한 면제 통로 · 형제와 같은 래칫 방침)를
각자 자기 머리에 한 벌씩 적어 두었다. 한 파일만 집으면 「어느 벌이 원본인가」를 정할 수
없어 같은 문장을 두 번 판정하게 된다.

검증 차량도 싸다 — 세 파일은 **자기 자신이 PR 에 붙는 게이트로 실행된다**
(`docs-mockup-render.yml` · `docs-journey-mockup.yml` · `docs-scenario-e2e.yml`).
주석만 지웠는지는 「돌려서 같은 값이 나오는가」로 바로 확인된다.

## 집계

| 파일 | 판정 전(지문 기준) | 판정 후 | 지문 기준 감소 | diff |
|---|---|---|---|---|
| `tools/check-mockup-render.py` | 107 | 53 | 54 | 96행 삭제 · 6행 재작성 |
| `tools/check-journey-mockup.py` | 62 | 33 | 29 | 33행 삭제 |
| `tools/check-scenario-e2e.py` | 44 | 26 | 18 | 23행 삭제 |
| **합계** | **213** | **112** | **101** | **152행 삭제 · 6행 재작성** |

범위 지문은 `86dee9cd…`(213) → **`8b41c689f8bb34951e205c72d855e4354a6f07fc9569e209ab481bcc1bc41ab7`**(112).

지문 기준 감소(101)와 diff 삭제 줄 수(152)가 다른 것은 **파이썬 docstring(`"""…"""`)이
지문에 보이지 않기** 때문이다(README 「지문과 사각지대」). 이 패스가 지운 docstring 은
51행이고, 그것들도 판정 대상이다 — 지문이 못 보는 자리라고 남겨 두면 같은 중복이 그 자리에
계속 쌓인다.

**동작 코드 무접촉**: 세 파일의 **AST 가 부모 `7a232f9` 와 동일**하다(`ast.parse` 후
module·함수·클래스 머리의 docstring Expr 만 정규화해 `ast.dump` 비교 — 3/3 IDENTICAL).
줄 수만 세는 필터나 「비주석 diff 0줄」 검사는 주석에 딸려 사라진 선언을 놓치므로 쓰지 않았다.
세 게이트의 출력도 부모와 **바이트 동일**하다(아래 「검증」).

## `tools/check-mockup-render.py` — 무엇을 왜 지웠나

### ① 파일 머리의 SSOT·면제 통로 서술 (4행)

「시각의 단일 소스는 `docs/mockups/` 다 … 판정이 붉으면 구현을 목업에 맞추거나 원장에
사유·해소 시점과 함께 등재한다. 원장이 유일한 면제 통로이며, 스크립트는 원장 자체의
무결성도 검사한다」.

- 앞 두 문장은 `docs/doc-tracker/2026-09.md` 「목업↔구현 대조 범위」와
  `docs/mockups/README.md` 가 규정하는 내용의 되풀이다(②).
- 마지막 문장은 **같은 머리의 M4 항목**이 그대로 말한다(①). 자기 자신을 세 줄 아래에서
  다시 인용하는 자리다.

### ② `data-sample` · `data-variant` 규약 전문 (22행)

`SAMPLE_OPEN` 위 8행과 `VARIANT_OPEN` 위 14행. 두 블록 다 **스스로 복원처를 지목한다** —
「규약 전문은 docs/mockups/README.md 「예시값 표기 규약」」 · 「… 「제공자 변이 표기 규약」」.
그 README 를 열면 같은 내용이 규칙 1·2·3 으로 나뉘어 예제 HTML 과 함께 있고, 주석이 든
논거까지 문장 단위로 겹친다.

| 주석이 말하는 것 | README 의 같은 문장 |
|---|---|
| 잎에만 붙이는 이유 — 「행 전체에 붙이면 같은 행의 제품 카피(예: `근거 없음` 태그)까지 함께 사라진다」 | 규칙 1 — 「행(`div.ev`) 에 붙이면 같은 행에 있는 제품 카피까지 함께 사라집니다 — 실제로 `근거 없음` 태그가 그렇게 묻힙니다」 |
| 「예시값으로 위장해 대조를 빠져나갈 수 없게 한다 — 상호작용하는 것은 제품 카피다」 | 규칙 2 — 「CTA 문구를 예시값으로 위장해 대조를 빠져나가는 길을 막기 위한 것이고, 이 금지는 게이트 규칙 **M6** 이 집행합니다」 |
| 변이의 비대칭 — 「M3A 에서 빠지고 M3B 의 건초더미에는 남는다 … 열거가 곧 허용 집합이다」 | 「붙은 요소는 **M3A(목업→구현)에서 빠지고, M3B(구현→목업)의 대조 집합에는 남습니다**」 + 「목업이 적어 둔 변이 집합이 곧 **구현이 그려도 되는 것의 전부**가 됩니다」 |
| 「면제가 아니라 **열거 의무**다. `data-sample` 과 다른 점이 여기다」 | 「**이것은 면제가 아니라 열거 의무입니다.** `data-sample` 과 갈리는 지점이 여기입니다」 |
| 「전진 행동은 변이로 감출 수 없다」 | 규칙 3 — 「전진 행동에는 붙일 수 없습니다 … 이 금지도 **M6** 이 집행합니다」 |

두 번째 근거: 그 주석의 포인터는 **이미 낡아 있었다**. 주석은 「제공자 변이 표기 규약」을
가리키는데 README 의 절 제목은 「**선택** 변이 표기 규약」이다. 되풀이한 주석이 원본만
고쳐질 때 조용히 거짓이 된다는 이 유형의 비용이 그대로 나타난 자리다.

### ③ M7 블록 머리의 5행 — 같은 머리 주석의 두 번째 벌

`APPBAR_MARKER` 위 5행(「앱바는 슬롯의 나열이다 … 카피 대조는 이 축을 볼 수 없다:
자리표시자는 글자가 없고 … 그래서 슬롯 **수**만 따로 센다」)은 파일 머리 「이 게이트가 보지
않는 것」의 첫 항목과 **같은 논증**이다 — 그쪽이 사건 기록(2026-09-18, 우측 자리표시자 부재
3건)까지 담고 있어 원본이다. 여기는 그 요약이다(①).

### ④ 인라인 규칙 마커 (10행)

`main()` 안의 `# ── M0 ──` · `# ── M1 ──` · `# ── M2 ──` · `# ── 원장 인덱스 ──` ·
`# ── M3A 목업 → 구현 ──` · `# ── M3B 구현 → 목업 ──` · `# ── M4 원장 무결성 ──` ·
`# ── M5 래칫 ──` · `# ── M6 예시값 표기 규약 ──` · `# ── M7 앱바 슬롯 ──`.

각 블록의 끝에 있는 `print(f"M4 원장 무결성 — 행 {len(ledger_rows)}건 …")` 가 규칙 태그와
이름을 그대로 출력하고, 파일 머리의 「규칙」 목록이 각 규칙을 한 항목씩 정의한다. 이것은
2026-09-18 판정이 `check-journey-prototype.js` 에서 같은 이유로 지운
「파일 머리 주석을 그대로 되풀이하는 인라인 마커」와 **같은 유형**이다
([2026-09-18-uncontested-harness-config.md](2026-09-18-uncontested-harness-config.md) ①).

### ⑤ 절 제목 구분선 (4행)

`── 마크다운 표 파싱 ──` · `── 목업 파싱 ──` · `── 구현 파싱 ──` · `── M1 매핑 발견 ──`.
바로 아래 선언(`table_after` · `mockup_steps` · `impl_copy` · `discover_screens`)이 이미 말한다.

### ⑥ 의존성·사용법 서술 (2행)

- 「의존성 0 (python3 stdlib). 형제 게이트 `check-journey-mockup.py` 와 같은 방침이다」 —
  `import` 다섯 줄이 전부 stdlib 이고(①), 워크플로 `docs-mockup-render.yml` 도
  「검사기는 python3 stdlib 만 쓰므로 setup 스텝이 없다」고 적는다(①).
- 「추출 결과는 `--verbose` 로 전부 출력된다. 무엇이 비교됐는지 눈으로 확인할 것」 —
  `verbose = "--verbose" in sys.argv` 와 `note()` 가 그 자체다(①). 남은 절반은 지식이
  아니라 리뷰 지시다.

### ⑦ 시그니처·본문을 되풀이하는 docstring (지문 밖 40행)

`is_reference`(「식별자·파일 경로는 카피가 아니라 참조다」 = 본문 `startswith("STP-") or
endswith((".tsx",…))`) · `drop_samples` · `sample_misuse` · `split_variants` ·
`variant_misuse`(「변이가 하나뿐이면 열거가 아니라 은닉이다」 = README 규칙 2) ·
`all_mockup_text` · `is_copy`(예시 수치 문단 = README 규칙 3) · `is_attribute_value` ·
`impl_copy` · `impl_haystack` · `discover_screens` · `mockup_steps` 의 `judged`/`pool` 설명 ·
`table_after` 의 첫 줄 · `jsx_text_nodes` 의 첫 줄.

`impl_haystack` 의 「'있는데 없다고 판정'하는 쪽으로만 튄다」는 파일 머리가 더 정확하게 적어
둔 것과 겹친다(「M3A 의 건초더미는 부분 문자열 검색이라 여전히 관대한 쪽으로 튄다」).

## `tools/check-mockup-render.py` — 판단이 갈려 **남긴** 것

- **파일 머리의 「규칙」 목록 M0~M7 전체.** 위 ④가 지운 인라인 마커의 **원본**이고,
  `docs-mockup-render.yml` 이 「무엇을 검사하는지는 이 헤더와 doc-tracker 에 있다」로 이 자리를
  가리킨다. 2026-09-18 판정이 `check-journey-prototype.js` 의 P1~P7 목록을 같은 이유로
  남긴 것과 같은 결정이다.
- **「이 게이트가 보지 않는 것」 블록 대부분.** M7 을 왜 넣었는지(카피 게이트의 사각지대 ·
  2026-09-18 우측 자리표시자 부재 3건), 인라인 CSS 길이 값이 카피로 새어 든 사건
  (2026-09-02 · 「원장에 넣었으면 거짓 부채였다」), 접근성 이름의 비대칭을 2026-09-18 에
  맞춘 경위와 그것이 **규칙 5 의 사각지대를 닫는 것이 아니라는** 단서. 전부 사건 기록이거나
  게이트의 한계 선언이라 코드·문서에서 복원되지 않는다.
  - 다만 그 안의 **M3B 추출 열거 3행은 좁혔다** — 「JSX 텍스트 노드 · 한글 포함 문자열
    리터럴 · JSX children 위치의 문자열 리터럴만 본다」는 `impl_copy()` 본문의 재진술이다(①).
    **그 열거가 낳는 결과**(영문 상수 라벨은 못 잡고, 그런 화면은 「대조 보류」에 있어야 하며
    보류 상한이 그 사실을 붙잡아 둔다)만 남겼다.
- **`# 한글 = 제품 카피 신호`(1행).** 위에서 머리의 열거를 좁혔으므로 이 휴리스틱의 근거가
  남는 자리는 여기 하나다. 이름과 정규식은 *무엇을* 하는지만 말하고 *왜 한글이면 카피인지*는
  말하지 않는다.
- **`jsx_text_nodes` 의 ⚠️ 3행** — 「TypeScript 제네릭도 `>`…`<` 사이에 걸린다. JSX 텍스트에는
  `;` 나 `=` 가 나타나지 않으므로 그것으로 코드를 가른다」. `CODE_ISH` 가 존재하는 이유이며
  오탐의 정체다.
- **`table_after` 의 「표 헤더를 키워드로 거르지 않는다 — 구분선 기준이 유일하게 안전하다」** —
  채택하지 않은 설계를 배제한 근거다. 코드에는 **없는 것**이라 읽어서 복원되지 않는다.
- **`ticked` 의 1행** — 「기계 대조는 백틱 토큰만 쓰고 나머지 산문은 사람용이다」. 원장 셀의
  문법 계약이고, 원장 행을 쓰는 사람이 알아야 하는 규칙이다.
- **M4 의 「대상 파일을 「현재 구현」 칸에서도 찾는」 2행** — 단계 단위 원장 행의 모양
  (대상 칸이 목업 파일이고 어느 화면이 그 단계를 그리는지는 구현 칸에 적힌다)을 설명한다.
  코드는 두 칸을 합치는 결과만 보인다.
- **M7 의 「앱바가 양쪽 다 없는 화면」 4행** — 목업이 `toprow` 를 쓰는 온보딩 계열을
  「구현이 앱바를 빠뜨렸다」와 어떻게 구분하는지. 합의 상태와 누락 상태가 코드에서
  같은 `None` 이라 주석 없이는 갈리지 않는다.

## `tools/check-journey-mockup.py` — 무엇을 왜 지웠나

### ① 절 제목·인라인 규칙 마커 (16행)

`── 1. 여정 문서 파싱 (SSOT) ──` · `── 2. 목업 페이지 파싱 ──` ·
`── R0 · README 기계 판독 구간 ──` · `── 예외 등재 (규칙 8) — SSOT 는 doc-tracker
「수용된 위험」 ──` · `── R1 ──` · `── R7 · 이관 래칫 ──` · `── R2 ──` ·
`── R3~R6 · 여정 페이지 내부 ──` · `# R3 단계 집합 양방향 일치` · `# R5 앵커 무결성` ·
`── R8 ──` · `── R9 · 집계 일치 ──` · `# 이관 완료 여정의 단계 표가 문서와 같은가` ·
`# 허브 요약 수치` · `── R11 ──` · `── 보고 ──`.

머리 docstring 의 R0~R11 목록이 각 규칙을 한 줄씩 정의하고, 각 블록의 `fail("R3", …)` 이
규칙 태그와 실패 문면으로 같은 내용을 말한다. 예외 등재 줄의 「SSOT 는 doc-tracker
「수용된 위험」」도 바로 아래 `TRACKER[TRACKER.index("## 수용된 위험"):…]` 가 그대로 말한다(①).

### ② R6·R10 의 규칙 재진술 (4행)

- R6 2행 — 「화면을 원본과 바이트 동일하게 못 박는 방식은 실제 입력 요소·상태 변형을 넣을
  길을 구조적으로 막는다」는 머리 docstring 의 마지막 문단이 더 길게 적어 둔 것이다.
  그 문단을 원본으로 남겼다.
- R10 2행 — 「문서 메타는 제품 화면과 같은 평면에 상시 노출되지 않아야 한다(규칙 5b).
  기본으로 접힌 `<details>` 안에만 둔다」는 머리의 R10 항목 + 두 `fail("R10", …)` 문면과
  같은 문장이다.

### ③ 여정 밖 분기(handoff) 설명의 잉여 벌 (7행)

같은 명제가 다섯 자리에 있었다 — `parse_pages` 안 2행, `STEP_OWNER` 위 2행, R4 루프 안 3행,
R5 블록 5행, `want` 계산 위 2행. **R5 블록의 3행을 원본으로 남기고** 나머지 넷 중 셋을
지웠다(`parse_pages` 2 · `STEP_OWNER` 2 · R4 3). R5 의 벌이 가장 완전하다 — 갈래의 끝이
대상을 선언한다는 규칙에 「**선언 대상의 실재 여부는 페이지가 아니라 여정 문서에서 확인한다
(자기참조 금지)**」까지 붙어 있고, 그 금지는 코드에서 복원되지 않는다.

`want` 계산 위 2행(「대상 여정에 페이지가 있으면 그 단계 앵커로, 아직 없으면 여정 문서를
reader 로 연다」)은 **바로 아래 세 줄의 삼항 연산이 문장 그대로**다(①).

### ④ 머리 docstring 의 세 번째 벌 (지문 밖 3행)

「규칙 5 의 (c)(d)(e) 는 정적 대조로 확인할 수 없다 — 파일을 읽어 속성만 세면 배선이 끊긴
버튼과 살아 있는 버튼이 구분되지 않는다. 그쪽은 DOM 하네스 `tools/check-journey-prototype.js`
가 같은 CI 게이트에서 집행한다」.

같은 논증이 세 곳에 있다 — `check-journey-prototype.js` 머리의 「왜 정적 체커로 안 되는가」
(가장 자세하다), `docs-journey-mockup.yml` 의 「2가 따로 있는 이유」, 그리고 이 줄.
원본은 하네스 자신의 머리다(①).

## `tools/check-journey-mockup.py` — 판단이 갈려 **남긴** 것

- **머리 docstring 의 SSOT 방향 도식과 R0~R11 목록.** 위 ①②가 지운 것들의 원본이다.
- **`parse_pages` 의 `<body>` 4행** — 「한 정규식으로 findall 하면 같은 태그의 두 번째
  `data-journey` 를 놓쳐(탐욕적 `[^>]*`) "정확히 1개" 규칙이 공허해진다 — **네거티브
  컨트롤이 잡아낸 결함이다**」. 뮤테이션 내성의 근거이자 실제 사고 기록이다.
- **R4 의 3행** — 「멤버십만 보면 공허하다(버튼의 대상을 바꿔도 자기 자신과는 늘 일치한다)」.
  왜 순서까지 대조하는지의 근거다. 머리의 R4 항목은 「이동 가능」까지만 말한다.
- **R3 의 딥링크 1행** — 섹션 `id` 와 `data-step` 이 같아야 **딥링크가 성립한다**는 이유
  (규칙 5e). 실패 문면은 「다르다」까지만 말한다.
- **R7 의 이관 래칫 3행** — 「화면 단위 목업과 그 이관 대기 원장은 2026-09-03 화면 ID 폐지로
  함께 사라졌다 … 남은 래칫은 이관 대기 상한 하나다」. 폐지된 축의 기록이고, 래칫이 왜
  하나뿐인지를 설명한다.
- **R8 의 세 블록** — `<script>/<style>` 안의 치환 템플릿이 링크로 오인된 자리(구체적
  오탐), `.nojekyll` 때문에 HTML 에서 `.md` 직결이 내려받기가 되는 함정(「실제로 밟은
  함정이다」), 허브→여정 단계 링크가 **아무도 검사하지 않는 사각이었다**는 기록.
- **R10 의 `<head>` 1행** — 제품 화면 평면의 경계 정의(title·meta 는 그 평면이 아니다).
- **R9 의 프래그먼트 2행** — 프래그먼트를 R8 이 아니라 R5 가 본다는 규칙 간 분담.
- **R11 의 6행** — 「R8 은 앵커만 보고 R9 는 숫자만 대조하므로 살아 있는 화면으로 들어가는
  링크 옆에 「시각화 없음」이 남아 있어도 둘 다 초록이었다」 + 「곁텍스트는 문면을 가리지
  않고 전부 잡는다 — 특정 문구만 막으면 같은 주장을 바꿔 쓰는 것으로 빠져나간다」. 규칙의
  존재 이유와 **왜 문면 필터를 두지 않았는지**다.

## `tools/check-scenario-e2e.py` — 무엇을 왜 지웠나

### ① 파일 머리의 SSOT·면제 통로 서술 (10행)

「시나리오의 단일 소스는 `docs/test/` 의 `### 시나리오 N:` 헤딩이다 … 붉으면 닫는 길은
셋뿐이다 — 전용 spec 을 만들거나, 「e2e 매핑」 절 예외 목록(규칙 4)이나 구현 대기 표
(규칙 6)에 등재하거나. 등재가 유일한 면제 통로이며, 스크립트는 등재 자체의 무결성도
검사한다」 + 「의존성 0 … 형제 게이트와 같은 방침이다」.

- SSOT 선언은 바로 아래 `HEADING_RE = re.compile(r"^### 시나리오 (\d+):")` 와
  `TEST_DOC_DIR` 이 그대로 말한다(①).
- 세 갈래 면제 통로와 규칙 번호는 `docs/doc-tracker/2026-09.md` 「e2e 매핑」 절이
  표 캡션으로 규정하고(②), 등재 무결성 검사는 **같은 머리의 S3 항목**이 말한다(①).
- 의존성 0 은 `import re · sys · pathlib` 셋(①)과 `docs-scenario-e2e.yml` 의 같은 문장(①).

### ② 사용법 지시 (1행)

「대조된 내역은 `--verbose` 로 전부 출력된다. 무엇이 비교됐는지 눈으로 확인할 것」 —
형제 파일과 같은 이유로 지웠다.

### ③ 절 제목·인라인 규칙 마커 (7행)

`── 실측: 시나리오 집합 ──` · `── 실측: spec 선언 ──` · `── 등재: doc-tracker 「e2e 매핑」
절 ──` · `# S3 — 등재 시나리오의 실재성과 버킷 간 배타성` · `# S3 — 매핑 표 ↔ 실제 파일
선언` · `# S4 — 집계·불변식` · `# S5 — 래칫`. 머리의 S0~S5 목록과 각 `fail("S4", …)` ·
`note(f"불변식 목표 …")` 가 같은 내용을 말한다.

### ④ 시그니처를 되풀이하는 docstring (지문 밖 5행)

`read_scenarios`(「docs/test/*.md → {문서 파일명: [시나리오 번호…]}」 = 타입 힌트) ·
`read_declarations` · `split_blocks` · `table_rows` · `collect`.

## `tools/check-scenario-e2e.py` — 판단이 갈려 **남긴** 것

- **머리의 S0~S5 목록 전체.** ③이 지운 마커의 원본이고 `docs-scenario-e2e.yml` 이 가리킨다.
  - 그 안의 S1 항목 한 줄(`// 검증 시나리오: <문서 파일명>#시나리오 <N>`)은 **기계 판독
    패턴이라 모델이 지문·판정에서 제외한다** — 손대지 않았다.
- **「이 게이트가 보지 않는 것」 3항목.** 「시나리오 문구와 단정 내용의 의미적 일치는 보지
  않는다 — 그것은 리뷰의 몫」, 「AC ↔ 시나리오 층은 범위 밖」, 「`e2e/smoke.sh` ·
  `playwright.config.ts` · `package.json` · `e2e/support/` 는 매칭 단위가 아니다」.
  게이트가 **하지 않기로 한 것**이라 코드에는 없다.
- **`slice_section`·`caption_numbers`·`find_block` 등 나머지 본문 주석 없음** — 이 파일은
  원래 인라인 주석이 적어 남길 것과 지울 것이 머리에 몰려 있었다.

## 증분 재판정 — 원장 9·10행에 #85 가 연 +7행

원장 9행(파이프라인·횡단 9파일)과 10행(프런트 데이터·셸 8파일)은 각각 말미에
「미판정 증분 없음」으로 끝나거나 그 상태로 등재돼 있었는데, **#85**(`7a232f9`, 제품 fix —
연결된 저장소 카드를 누르면 Repository URL 칸을 채운다)가 그 두 범위에 주석 **7행**을
얹었다. 원장 규약대로 **새 행을 만들지 않고** 9·10행의 결과 칸을 갱신한다.

이 7행은 **10차 패스가 바로 그 파일들에서 지운 유형이 다시 들어온 것**이다.

| 파일 (원장 행) | 증분 | 판정 | 근거 |
|---|---|---|---|
| `e2e/tests/sc01-02-repo-out-of-scope.spec.ts` (9행) | +1 | 제거 | 「목업처럼 목록의 저장소를 누르면 Repository URL 칸이 채워진다 — 직접 타이핑하지 않는다」 — **바로 아래 세 줄이 그 문장 자체**다(`cards.filter(…).click()` → `expect(getByTestId('repo-url')).toHaveValue('github.com/…')`). 「목업처럼」은 목업 카피 인용이다(①②) |
| `frontend/src/HomeRepositories.tsx` (10행) | +5 | 제거 | `pick()` 의 JSDoc 4행 — ⑴「목록의 저장소를 누르면 Repository URL 칸이 채워진다」는 본문 한 줄(`edit(setRepoUrl)('github.com/' + fullName)`)의 재진술 ⑵「목업 `renderHome()` 의 repo-item 클릭 핸들러와 같은 동작」은 목업 인용(②) ⑶「`edit` 을 거쳐 이전 추정을 무효화한다」는 **여섯 줄 위** `edit()` 의 본문(`setEstimate(null); setError(null)`)이 그대로 말한다. 인라인 1행(「카드 클릭으로 번지지 않게 — 이 버튼은 진행 상황만 연다」)은 `e.stopPropagation()` + 부모의 `onClick` + 버튼 라벨 `진행 상황` 이 말한다(①) |
| `frontend/src/index.css` (10행) | +1 | 제거 | 「누르면 연결 폼을 채우는 저장소 카드 (목업 `.repo-item:hover`)」 — 선택자 `.card.pickable:hover` 가 절반을, 나머지는 목업 인용이다(①②) |

**증분 7행 전건 제거 · 유지 0행.** 이 범위에서 유지 0 이 나온 것은 처음인데, 증분이
「같은 슬라이스가 두 파일에 같은 동작을 세 번 적은」 형태라 갈릴 여지가 없었다.

### 두 행은 판정 전 값으로 **정확히 되돌아왔다**

9차·10차 패스가 「줄 수 일치를 원상 복구로 읽지 말 것」이라고 경고한 자리인데, 이번에는
**줄 수와 지문이 둘 다 되돌아왔다**.

| 원장 행 | #85 이전 (`e340bdc` 실측) | #85 이후 (main tip) | 이 패스 뒤 |
|---|---|---|---|
| 9행 | 140 / `03babd782b09…` | 141 | **140 / `03babd782b09…`** |
| 10행 | 80 / `641e9457a5fe…` | 86 | **80 / `641e9457a5fe…`** |

「되돌아왔다」를 **부모 트리 실측으로 확증**했다 — `e340bdc`(#85 의 부모)에서 같은 범위를
재계산한 값이 이 패스 뒤의 값과 바이트 동일이다. #85 가 그 두 범위에서 건드린 것이 주석
7행뿐이었고, 그 7행이 전건 제거됐기 때문이다. 그래서 두 행의 **줄 수·지문 칸은 그대로 두고**
증분 재판정 사실만 결과 칸에 더했다.

## 검증

1. **세 게이트의 출력이 부모와 바이트 동일**(`diff` 0) — `check-mockup-render.py`
   (M0~M7 전 규칙 통과) · `check-journey-mockup.py`(R0~R11) · `check-scenario-e2e.py`.
   게이트가 자기 소스의 주석을 읽지 않는다는 것을 실행으로 확인한 것이기도 하다.
2. **DOM 하네스도 통과** — `node tools/check-journey-prototype.js` 가 「여정 프로토타입 5개 ·
   단언 408건 실행 · 통과」. 이 패스는 그 파일을 건드리지 않지만 같은 워크플로의 두 번째
   잡이므로 함께 돌렸다.
3. **AST 동일 3/3** — 위 「동작 코드 무접촉」.
4. **네거티브 컨트롤** — 같은 계측을 부모 `7a232f9` 에서 돌리면 값이 다르다
   (범위 213/`86dee9cd…` vs 112/`8b41c689…`, 전역 2,761 vs 2,653) ⇒ 측정이 변화를 실제로 구분한다.
5. **포지티브 컨트롤** — 원장 1~8행 전건을 이 패스의 트리에서 재계산해 줄 수·지문이
   기재와 **8/8 바이트 동일**이다(587 / `6b77b15a…` · 140 / `ddacce0b…` · 142 / `93be69ea…` ·
   141 / `e38dcf20…` · 190 / `632b0475…` · 112 / `e2ff313a…` · 132 / `a0354a08…` ·
   140 / `d46c9262…`) ⇒ 9·10행의 「전건 일치」가 측정 정지의 산물이 아니다.

## 허브(`docs/index.html`)도 함께 고쳤다

`docs/**.md` 가 하나 늘었으므로 `check-journey-mockup.py` 의 **R9** 가 허브 요약
`Documents` 수치와 `reader.html?doc=` 링크 집합을 실측과 1:1 대조한다. 이 패스 파일을
링크로 더하고 집계를 **36 → 37** 로 올렸다. 빠뜨리면 R9 가 실패한다. 허브는 주석 지문
범위 밖이라 판정 수치에는 영향이 없다.

## 범위 밖으로 남긴 것 (다음 패스가 받는다)

이 패스 뒤 미판정 잔여는 **46파일 / 737행**이다(리베이스 후 부모 `b2724da` 기준). 잔여는 전역 지문의 파일 목록에서 판정
66파일을 **집합으로 뺀** 값이다 — 「행 없는 파일」만 세거나 수치를 빼서 구하지 않았다
(`format.ts` 처럼 주석이 0행이 되어 지문 파일 집합에서 빠진 판정 파일이 있어 두 방식은
어긋난다).

1. **`backend/migrations/*.sql` 8파일 / 139행** — 본문 「적용된 마이그레이션」 절의
   전용 PR · 수동 repair · **사람 승인 게이트**를 거치는 별도 패스다. 다른 정리와 섞지
   않는다. 이번 트리거로 움직이지 않았다.
2. **자유 풀 38파일 / 598행** — `tools/` 의 옛 4파일이 닫혀 남은 것은 **backend 쪽과 sc04
   계열 e2e 로 갈린다**(+ 아래 3 의 신규 파일). 큰 후보: `backend/tests/worker.rs` 35 · `backend/src/github_app.rs` 35 ·
   `sc04-12` 33 · `backend/tests/llmkey.rs` 33 · `backend/src/github.rs` 33 · `sc04-13` 30 ·
   `sc04-04` 24 · `sc04-03` 23 · `sc02-02` 23 · `backend/tests/auth.rs` 22.
   - **다음 축으로 「GitHub App · 인증 경계」가 잡힌다** — `github_app.rs` 35 ·
     `github.rs` 33 · `github_api.rs` 15 · `github_tokens.rs` 7 · `auth.rs` 17 ·
     `tests/github.rs` 15 · `tests/auth.rs` 22 + sc04 계열. 상류 API 의 문서화되지 않은
     거부 조건이 몰려 있는 자리라 **유지 판정이 많이 나올 축**이고, 그만큼 판정 밀도가
     낮다는 것을 예상해 두어야 한다.
   - `sc04-03` · `sc04-11` 에는 `rct_20260916-0002` 를 인용한 작업 흔적 2행이 남아 있다 —
     정책이 이름 붙인 제거 유형 ③의 잔여다.
3. **`tools/check-data-format-change.py` 58행** — #64 가 **이 패스를 판정하는 동안 머지돼**
   (`b2724da`) 잔여에 들어온 파일이다. 계획 시점 main 에 없었으므로 이 패스의 범위가 아니고,
   재감지가 새 task 로 잇는다. 위 자유 풀 38파일 안에 포함돼 있다.

   예고했던 이동폭은 59행이었는데 실제는 **58행**이다 — 신규 파일의 shebang 이 모델의 제외
   패턴 `:#!/` 에 걸린다. 그 패턴은 **`경로:내용` 형태의 줄**에 대고 맞추므로 파일 내용만
   보고 세면 한 줄이 남는다. 신규 파일의 주석 수를 미리 셀 때 주의할 지점이다.
