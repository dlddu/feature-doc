# 판정 상세 — 문서 편집 축 (슬라이스 6a 가 들여온 새 파일 6개) · 2026-09-21

reconciler task `rct_20260921-0007`(모델 `tbm_feature-doc-comment-redundancy`). 자매 모델
`tbm_feature-doc-docs-impl` 의 슬라이스 6a(**PR #92**, `51daa9c`, AC3.1 LLM 보조 문서 수정)가 판정
대상 범위에 주석 **188행을 순수 유입**(제거 0)시켰다. 그중 마이그레이션 `0009_feature_doc_edits.sql`
30행은 본문 「적용된 마이그레이션」 절의 사람 게이트 몫이라 **이 패스가 다루지 않는다**. 나머지
158행을 한 패스로 판정했다 — **새 파일 6개 / 143행**(이 파일의 본문)과 **판정 완료 원장 행 안으로
들어온 증분 5파일 / 15행**(원장 1·2·5·10행 — 각 원래 패스 파일에 절을 더했다, 아래 「증분」 절).

## 범위와 결과

| 파일 | 유입 | 제거 | 유지 | 비고 |
|---|---|---|---|---|
| `backend/src/doc_edit.rs` | 80 | **51** | 29 | 모듈 머리 16행 → 요약 1행 · `///` 필드·fn doc 대부분 |
| `backend/tests/doc_edit.rs` | 14 | **13** | 1 | 모듈 머리 8행 → 요약 1행 · 나머지는 테스트 이름·단정 메시지가 복원 |
| `e2e/tests/sc03-01-llm-assisted-edit.spec.ts` | 20 | **11** | 9 | Isolation 4행 → 1행(13차 패스 규약) · 단정 옆 주석 8행 |
| `e2e/tests/sc03-02-rejected-suggestion-avoided.spec.ts` | 15 | **8** | 7 | Isolation 4행 → 1행 · 단정 옆 주석 5행 |
| `frontend/src/DecideDiff.tsx` | 8 | **4** | 4 | 머리 문단의 편차 원장 인용 절반 · 사유 칸 인라인 2행 |
| `frontend/src/RequestEdit.tsx` | 6 | **0** | 6 | 화면 불변식 머리 2행 · 판단 갈려 남긴 것 1건(아래) |
| **합** | **143** | **87** | **56** | 범위 지문 **56 / `b9e6778d…`** |

증분 5파일(원장 행 안): `frontend/src/api.ts` 5 → 제거 3 · 유지 2 / `backend/src/analysis.rs` 4 →
제거 4 / `frontend/src/App.tsx` 2 → 제거 2 / `backend/src/config.rs` 2 → 제거 2 /
`deploy/e2e/kustomization.yaml` 2 → 제거 2 — **15행 중 제거 13 · 유지 2**. 합쳐 **순 제거 100행**.

**diff 기준**: 10파일 103행 삭제 · 3행 삽입(두 spec 의 Isolation 한 줄 · `DecideDiff.tsx` 4행 재작성).
지문 감소(100)와 diff 삭제 줄 수가 갈리는 것은 재작성 3행 때문이다 — 4행이 1행이 되는 것을 지문은
−3 으로, diff 는 −4 +1 로 센다.

## 이 축의 복원 경로가 유난히 두꺼운 이유

#92 는 자기 설계 판단을 **세 자리에 이미 적어 두었다**: ② `docs/doc-tracker/2026-09.md` 의 변경 이력
행(「제안은 큐를 타지 않는다」 · 「자동 문서를 고치지 않는다」 · 「거부는 기록이다」 세 문단이 코드
주석과 문장 단위로 같다) · ③ PR #92 본문 「설계 판단 셋」(같은 세 문단) · ① 마이그레이션
`0009_feature_doc_edits.sql` 의 머리 주석(`content_hash` 를 왜 건드리지 않는가 · 거부를 왜 같은 표에
두는가 · `scenario_index` 0-based · `source` 어휘 · `reason` CHECK). 그래서 `doc_edit.rs` 모듈 머리
16행은 그 세 자리의 네 벌째였고, 화면·spec·테스트의 인라인 주석은 다섯·여섯 벌째였다. 0009 주석은
사람 게이트 몫이라 이 패스가 손대지 않으며, **저장 계약의 정본은 그 파일이다** — `doc_edit.rs` 머리가
스스로 「이유는 0009 마이그레이션 주석에 있다」고 적어 둔 그대로다.

## `backend/src/doc_edit.rs` — 제거 51행

### 모듈 머리 `//!` 16행 → 1행 (제거 15)

- 유지: `//! AC3.1: 읽던 사람이 한 줄로 부탁하면 그 시나리오를 고쳐 주는 흐름.` — 모듈 요약 1행.
- 제거 「세 번의 탭이 전부다 … 제안은 큐를 타지 않고 이 프로세스가 직접 모델을 부른다. 워커를
  쓰면 네 번째 화면이 생겨 여정이 못 박은 3탭(F8)이 깨진다 … 한 번의 호출이고 사용자가 화면
  앞에서 기다린다」 5행 + 빈 `//!` 1 — ② doc-tracker 슬라이스 6a 행 「**제안은 큐를 타지 않는다**
  — 여정이 못 박은 3탭(F8)이 「요청함·기다리는 중」 화면을 허용하지 않으므로 API 프로세스가 직접
  한 번 부른다(분석 파이프라인이 워커에 있는 이유인 다단계·장시간이 여기엔 없다)」 · ③ PR #92
  「설계 판단 1」 축자.
- 제거 「승인하기 전에는 아무것도 바뀌지 않는다 … 승인된 행만 읽는 시점에 겹쳐진다([`overlay`]).
  자동 문서를 직접 고치지 않는 이유는 0009 마이그레이션 주석에 있다」 3행 + 빈 1 — ① 0009 머리
  「왜 `analysis_documents` 의 행을 직접 고치지 않는가」 문단 · ② doc-tracker 「**자동 문서를 고치지
  않는다**」 · ③ PR 「설계 판단 2」 · ① `overlay` doc(유지, 아래).
- 제거 「거부는 버려지지 않는다. AC3.1 의 검증 방법이 「거부된 제안은 기록되어 다음 제안 시
  회피된다」이므로 … 화면도 같은 값을 읽어 사람에게 먼저 알린다」 4행 + 빈 1 — ② PRD
  `03-doc-management.md` AC3.1 검증 방법 원문 인용 · ② doc-tracker 「**거부는 기록이다** — 사유와
  그때의 문면이 다음 호출의 프롬프트로 들어가고 … 요청 화면이 같은 값을 읽어 사람에게 먼저
  알린다」 · ① 0009 「거부된 제안도 보존해야 하는 기록」 문단.

### 선언·필드 doc (제거 22)

| 줄 | 복원 경로 |
|---|---|
| `SOURCE_USER_LLM` 「AC3.4 의 출처 중 이 흐름이 남기는 값」 | ① 이름·값 `"user_llm"` · ① 0009 `source` 문단(어휘 셋과 그 이유) — AC 꼬리표 |
| `MAX_AVOIDED` 「한 요청이 회피 목록으로 들고 갈 수 있는 거부 건수의 상한」 + 빈 `///` | ① 이름 · `avoid_list` 의 `.take(MAX_AVOIDED)` — 뒤따르는 2행(상한의 목적 · 최근 것부터인 이유)은 **유지** |
| `routes()` 「feature key 는 경로 구분자를 품으므로 본문·쿼리로 받는다(`analysis` 와 같은 사유)」 | ① `analysis.rs:79` 가 같은 문장의 정본(주석 스스로 가리킨다) — 2벌째 |
| `ContextView.rejected_count` 「… 0 이면 화면은 그 안내를 그리지 않는다」 | ① 이름 · ① `RequestEdit.tsx` 의 조건 렌더 · `sc03-02` 첫 단정 |
| `ContextView.rejected_reason` 「가장 최근 거부의 사유 …」 | ① `rejected.first()` + `rejections()` 의 `ORDER BY decided_at DESC` |
| `ProposalView.source` 「AC3.4 의 출처. 화면의 「바꾼 주체」가 이 값을 읽는다」 | ① 이름 · `api.ts` 의 같은 문장(함께 제거) · ① 0009 `source` — AC 꼬리표 |
| `ProposalView.request` 「사람이 적어 보낸 한 줄. 이력에 남는 값이라 되돌려준다」 | ① 0009 「한 행이 제안 하나의 일생이다 — 무엇을 고쳐 달라 했고(`request`)」 · `sc03-01` 단정 메시지 「무엇을 부탁했는지도 이력이다」 |
| `ProposalView.after` 「그 자리에 설 시나리오들. 한 건이면 고쳐 쓴 것이고, 두 건 이상이면 …」 | ① `lines()` doc 이 이 구분의 정본(유지) · `SYSTEM` 프롬프트 「one when … two when …」 — 4벌 중 하나 |
| `ProposalView.removed`/`added` 「사라지는 문장과 들어오는 문장. 화면의 diff 가 …」 | ① 이름 · `lines()` 반환 |
| `ProposalView.changed_lines` 「배지가 읽는 수 — 지워지는 줄과 들어오는 줄의 합」 | ① `view()` 의 `removed.len() + added.len()` |
| `FeatureQuery.scenario` 「고칠 시나리오의 자리(0-based). 화면은 사람이 누른 그 줄을 보낸다」 | ① 0009 「`scenario_index` 는 … 자리(0-based)」 정본 · **낡음**: `App.tsx` 는 항상 `0` 을 보낸다(PR #92 「범위 밖」이 명시) — 제거 근거 강화 |
| `ProposeReq.request` 「… 빈 요청은 모델을 부르지 않는다」 | ① 바로 아래 `if request.is_empty() { return Err(BadRequest(…)) }` |
| `DecisionReq.decision` 「`approve` 또는 `reject`」 | ① `decide()` 의 `match` 가지 |
| `DecisionReq.reason` 「거부 사유. 거부에는 반드시 있어야 한다 — 없으면 다음 제안이 무엇을 피할지 모른다」 | ① 0009 `reason` CHECK 문단(정본) · ① `decide()` 오류 문자열 「왜 이게 아닌지 한 줄이 있어야 다음 제안이 그 방향을 피합니다」 — **여섯 벌**(0009 · 오류 문자열 · 이 줄 · `DecideDiff.tsx` · `tests/doc_edit.rs` · `sc03-02`) 중 정본 둘만 남긴다 |
| `context()` 「편집 요청 화면이 열릴 때 필요한 것 전부: 고칠 대상과, 이미 거부된 방향」 | ① `ContextView` 필드 · 라우트 `edit-context` |
| `propose()` 「한 줄의 요청을 제안 하나로 바꾼다. 문서는 아직 그대로다」 | ① 함수 본문(`analysis_documents` 에 쓰지 않는다) · `api.ts` `proposeEdit` JSDoc(export 함수 요약 1줄이라 **그쪽을 정본으로 유지**) |
| `proposal()` 「이미 만들어진 제안 하나. 결정 화면이 새로고침을 견디는 근거다」 | ① `routes()` 의 「제안도 주소를 가진다 — … 새로고침이 같은 제안을 다시 그린다」(유지, 정본) — 4벌(라우트 · 이 줄 · `App.tsx` · `sc03-01`) |
| `decide()` 「승인이면 겹쳐 읽히기 시작하고, 거부면 다음 제안이 피해야 할 것이 된다」 | `api.ts` `decideEdit` JSDoc 과 같은 문장(export 함수 요약이라 그쪽 유지) · ① `overlay`·`rejections` 의 `status` 필터 |
| `edit_row()` 「… 분석 소유권은 부른 쪽이 이미 확인했다」 | ① 호출자 둘 다 `owned_analysis` 를 먼저 부른다 — 호출자 재진술(1행 증분 재판정 ② 선례) |
| `rejections()` 「사람이 거부한 제안들, 최근 순으로 `(고쳐 쓴 then, 사유)`」 | ① 반환 타입 · `ORDER BY decided_at DESC` |
| `avoid_list()` 「프롬프트와 스텁이 함께 보는 회피 목록」 | ① 호출자 `prompt(…, &avoid)` · `stub_edit(…, &avoid)` |
| `scenarios_of()` 「… 편집은 자리로 지목되므로 순서가 곧 주소다」 | ① 0009 「시나리오에 안정 식별자가 없기 때문 … 자리로 잡아도 되는 이유」 정본 |
| `schema()` 「모델이 답할 모양 — 그 자리에 설 시나리오들. 세 칸 밖의 것은 받지 않는다」 | ① `additionalProperties: false` · `required` 세 칸 |
| `proposed()` 「답에서 세울 수 있는 시나리오만 꺼낸다. 하나도 읽히지 않으면 제안이 아니다」 | ① `filter_map` · `(!list.is_empty()).then_some` · 테스트 `an_empty_answer_is_not_a_proposal` 의 doc(유지 — 「문서를 비우는 승인이 되어서는 안 된다」는 이유가 거기에만 있다) |

### `overlay` doc 8행 → 4행 (제거 4)

- 유지: 요약 「승인된 편집을 자동 문서 위에 겹쳐 읽는다」(pub) + 「저장은 건드리지 않는다 — 문서를
  내보내는 자리에서만 겹친다. 같은 자리에 승인이 여러 번 쌓였으면 오래된 것부터 적용되므로
  마지막 승인이 이긴다」 2행 — 뒷문장은 `ORDER BY created_at, rowid` + `splice` 의 합성 결과라
  코드 한 줄이 말하지 않는다(7행 패스가 `(created_at, rowid)` 정렬 이유를 유지한 선례).
- 제거: 「자리를 찾지 못한 편집(… 시나리오 수가 줄어든 경우)은 조용히 건너뛴다. 그 상황은 자동
  결과와 사용자 편집의 충돌이고, 어느 쪽을 택할지는 AC3.5 가 사람에게 묻기로 한 문제라 여기서
  임의로 정하지 않는다」 3행 + 빈 1 — ① `if at >= list.len() { continue; }` · ① 0009 「자동 결과와
  사용자 편집이 같은 자리에서 갈릴 때 어느 쪽을 택할지는 사람에게 묻기로 한 문제이고, 그때 묻는
  쪽이 이 표를 입력으로 삼는다」 · ② PRD AC3.5 · ③ PR 「설계 판단 2」 끝문장.

### 인라인·스텁·테스트 (제거 10)

| 줄 | 복원 경로 |
|---|---|
| `overlay` 본문 「자리 하나가 여럿이 될 수 있으므로 갈아 끼우는 것이 아니라 그 구간을 바꾼다」 | ① 바로 아래 `list.splice(at..at + 1, replacement)` — 뒤따르는 「출처·근거는 그 자리에 있던 값을 물려준다 — 사람이 고친 것은 문장이지 …」 2행은 **유지**(설계 결정, `template.clone()` 이 이유까지는 말하지 않는다; `tests/doc_edit.rs` 의 같은 문장은 이쪽을 정본으로 제거) |
| `stub_edit` doc 「스텁의 답. 요청 문장을 결과 칸 뒤에 이어 관측 가능한 변화 하나를 만들고, 「추가」를 부탁받았으면 원래 시나리오를 남긴 채 한 건을 더 세운다」 2행 + 빈 1 | ① `before.then + " — " + request` · `request.contains(ADD_HINT)` 분기 — 뒤따르는 「회피는 스텁에서도 규칙이다 … 그러지 않으면 시나리오 2 가 스텁의 우연으로 통과한다」 2행은 **유지**(충실도 경계 — 이 명제의 정본, `sc03-02` 머리·doc-tracker·PR 이 이 자리를 가리킨다) |
| 테스트 doc 「사례가 늘어난 제안은 더해진 것만 줄로 낸다 — 남아 있는 시나리오는 지워지지 않는다」 | ① fn 이름 `an_added_scenario_shows_only_the_new_line` · 단정 메시지 |
| 테스트 doc 「「추가」를 부탁하면 원래 시나리오가 남고 한 건이 더 선다(테스트 문서 03#시나리오 1)」 | ① fn 이름 `asking_for_another_case_keeps_the_original` · ② 테스트 문서 03 시나리오 1 |
| 테스트 doc 「거부된 문면이 다시 나오지 않는다 — 회피가 규칙이라는 것의 기계 확인」 | ① fn 이름 `the_stub_avoids_a_rejected_answer` |

### 유지 29행 — 사유

`status` 「0009 의 CHECK 와 같은 값이어야 한다」(저장소 제약 — 어긋나면 INSERT 가 CHECK 에 걸린다) ·
`MAX_AVOIDED` 의 목적·순서 2행 · `MAX_PROPOSED` 「한 번의 요청이 문서를 통째로 다시 쓰는 일은
「한 줄로 부탁한다」가 아니다」 2행(상한 3 의 이유) · `routes()` 「제안도 주소를 가진다 …」 2행(정본) ·
`Sentences` 요약 1행(pub) · `propose()` 「이 사용자의 키로 부른다 — … 평문은 이 호출 동안만 산다」
(자격증명 비노출 불변식이 **이 자리에서** 지켜지는 이유 — `worker_api.rs` 의 같은 유형을 1차 패스가
유지) · `overlay` 4행 · `overlay` 본문 「출처·근거는 물려준다」 2행 · `rejections` 「회피의 단위는
「그때 내놓은 마지막 문장」이다 — 스텁·프롬프트가 함께 보는 값」(둘이 같은 단위를 봐야 하는
계약) · `document_of` 「고칠 대상도 그 위에 얹을 제안도 사람이 지금 보는 문장 기준이어야 한다」
(왜 여기서 겹쳐 읽는가 — 설계 결정) · `lines()` doc 6행(칸 단위 vs 시나리오 단위의 이유 — `after`
필드·`api.ts`·`schema` 의 사본 넷이 이쪽을 정본으로 걷혔다) · `ADD_HINT` 2행(스텁이 한 낱말로
가른다는 충실도 경계) · `stub_edit` 「회피는 스텁에서도 규칙」 2행 · 테스트 「세울 시나리오가 하나도
없는 답은 제안이 아니다 — 문서를 비우는 승인이 되어서는 안 된다」(이유가 이 자리에만 있다) ·
`// mock-exception: LLM-01` 은 기계 판독이라 지문·판정 밖.

**판단이 갈려 남긴 것**: `MAX_AVOIDED` 의 「프롬프트가 무한히 길어지지 않게」 — 상한이 프롬프트
길이를 묶는다는 것은 코드에서 따라오지만 그것이 *목적*이라는 서술은 어디에도 없다(1건).

## `backend/tests/doc_edit.rs` — 제거 13행

- 모듈 머리 8행 → 요약 1행 「LLM 보조 수정(AC3.1)과 그 변경의 출처 보존(AC3.4) — 라우터를 그대로
  돌려 본다」. 제거 「인수 문서는 워커의 `/internal` 경로로 넣는다(형제 테스트와 같은 방침) — 손으로
  행을 꽂으면 픽스처가 실제 워커 산출물과 어긋날 수 있고 …」 2행 — 주석 스스로 「형제 테스트와
  같은 방침」이라 가리키는 `backend/tests/dependencies.rs:3-4`(6차 패스 유지분)가 정본. 제거
  「브라우저에서 관측되는 3탭 흐름은 e2e `sc03-01`·`sc03-02` 가 지킨다. 이 파일이 지키는 것은 …
  승인 전 문서 불변 · 승인 후 겹쳐 읽기 · 거부의 기록과 회피 · 남의 분석에는 닿지 않음」 3행 —
  ① 네 테스트의 fn 이름이 그 목록이다 · ② doc-tracker e2e 매핑 표. 빈 `//!` 2행이 딸려 나갔다.
- `analysis_with_document()` 「인수 문서가 서 있는 분석 하나. 편집은 그 문서 위에서만 일어난다」 —
  ① fn 이름. `urlencoding()` 「feature key 는 경로 구분자를 품는다 …」 — ① `replace('/', "%2F")` ·
  `analysis.rs:79` 정본의 3벌째. 인라인 「근거는 물려받는다 — 사람이 고친 것은 문장이지 …」 —
  ① 바로 아래 `assert_eq!(after[1]["evidence"], before[0]["evidence"])` · `overlay` 본문 정본.
  「사유 없는 거부는 받지 않는다 — …」 — ① `BAD_REQUEST` 단정 · 0009 정본. 「사람에게 먼저
  알린다」 — ① `rejectedCount`·`rejectedReason` 단정. `another_users_analysis_is_not_editable` 의
  「AC4.7 — 남의 분석은 없는 것과 같다 …」 — ① fn 이름 · `NOT_FOUND` 단정 · `analysis.rs`
  `owned_analysis` doc(「404 not 403 … AC4.7」 정본).

## 두 spec — 제거 19행

- **Isolation 4행 → 「Leases the analysis worker — lease rules in `e2e/support/cluster.ts`.」
  1행(각 −3).** 13차 패스(`2026-09-20-pipeline-cross-cutting-axis.md` ②)가 정한 규약이다 — 그 문단
  자신이 가리키는 `e2e/support/cluster.ts:1-11` 에 더 정확하고 긴 정본이 있고(원장 3행 유지분),
  13차가 3개 spec 을, 14차가 `sc02-02` 를 이 한 줄로 줄였다. 6·8차 패스가 각자 범위의 4행 블록을
  「동시성 계약」으로 유지한 것과 어긋나 보이지만, 그 두 판정은 13차가 정본을 지목하기 **전**이고
  13차가 「나머지 10개는 후속 패스의 명시적 후보」라 적어 둔 상태라, 새로 들어온 파일에는 최신
  규약을 적용하는 것이 뒤집기가 아니다. **이미 판정된 spec 의 Isolation 블록 12개는 이 패스 범위 밖**이며
  후속 후보로 그대로 남는다(아래 「범위 밖」).
- `sc03-01` 제거 8: `PHONE` 의 「목업이 「모바일 우선」으로 그리는 폭. AC4.4 의 검증은 아직 이 spec 의
  일이 아니다」 — ② doc-tracker 매핑 행 「390px 폭」 · `// 검증 시나리오:` 선언이 이 spec 의 AC 를
  이미 못 박는다. 「읽던 자리(의존성)에서 진입한다 — 목업이 그 화면에 요청 CTA 를 그린다」 — ②
  doc-tracker 「의존성 화면의 요청 CTA」 · ③ PR 「범위 밖」 3번째. 「요청이 비어 있는 동안에는 보낼 수
  없다 — …」 — ① 바로 아래 `request-empty`·`toBeDisabled()` 단정. 「승인 전에는 아무것도 바뀌지
  않는다」 — ① 단정 메시지 「승인하지 않았는데 문서가 이미 바뀌었다」. 「제안은 서버가 들고 있는
  값이라 주소를 가진다 …」 — `doc_edit.rs` 라우트 정본 · ① 단정 메시지. 「제안된 문장이 그대로
  문서에 섰다 — 화면이 아니라 저장이 기준이다」 — ① 단정 · 머리 문단 「결과 확인만 저장된 문서를
  읽는다」(유지)의 재진술. 「원래 시나리오는 지워지지 않는다(「추가」는 대체가 아니다)」 — ① 단정
  메시지 「원래 시나리오가 사라졌다」 · ② 테스트 문서 03 시나리오 1. 「AC3.4 — 변경의 출처가 「사용자
  by LLM」으로 남는다」 — ① 단정 메시지 「AC3.4 가 요구하는 출처」 · ② 테스트 문서.
- `sc03-02` 제거 5: 「첫 진입에는 거부 이력이 없다 — 안내가 서면 그것이 거짓이다」 — ①
  `toHaveCount(0)` · ② doc-tracker 「첫 진입에는 거부 안내가 없다」. 「사유 없는 거부는 받지 않는다 —
  첫 탭은 사유 칸을 연다」 — ① 다음 세 줄(클릭 → `reject-reason` 가시 → 입력 → 클릭) · 0009 정본.
  「거부는 문서를 바꾸지 않는다」 — ① 단정 메시지. 「다시 같은 자리에서 같은 부탁을 한다」 — ① 같은
  URL `goto` · 같은 `REQUEST`. 「사람에게 먼저 알린다 — 몇 건을 거부했고 그 사유가 무엇이었는지」 —
  ① `rejected-count`·`rejected-reason` 단정.
- **유지**: 두 spec 머리의 판정 이유 문단(`sc03-01` 「3탭을 세는 것이 이 spec 의 일이다 — 흐름을 API
  로 질러가면 그 단정이 사라진다 …」 3행 · `sc03-02` 「회피를 관측한다 — 모델의 선의가 아니라
  규칙이어야 하므로 같은 요청을 두 번 …」 4행) — 테스트 **설계** 의 이유라 doc-tracker 매핑 행
  (무엇을 관측하는가)로는 복원되지 않는다(13차가 `sc01-03` 머리 「기대값을 상수로 박지 않는다」를
  유지한 선례). `sc03-01` 의 `// ── 탭 N ──` 3행 — 구분선 모양이지만 이 spec 이 세는 것이 바로 그
  세 번의 탭이고, 아래 `.click()` 셋 중 어느 것이 몇 번째 탭인지는 코드가 말하지 않는다 — **판단이
  갈려 남긴 것**(2건째).

## 두 화면 — 제거 4행

- `DecideDiff.tsx` 머리 6행 → 4행: 「승인하기 전에는 문서가 바뀌지 않는다 — 이 화면이 그리는 것은
  제안이고, 적용은 서버가 승인을 받은 뒤에 한다」까지 남기고(10차 패스가 `FeatureAcceptance` 머리의
  「Every sentence on this screen came from the server」류 화면 불변식을 유지한 선례) 뒤의 「거부에
  사유를 받는 칸은 목업에 없다: 테스트 문서가 「사유와 함께 거부」를 요구해(권위 순서상 목업보다
  위) 한 칸을 더 그렸고, 그 차이는 원장에 등재돼 있다」를 걷었다 — ② doc-tracker 편차 원장 행
  (`frontend/src/DecideDiff.tsx` · 「권위 순서에서 목업보다 위인 테스트 문서가 … 「사유와 함께
  거부」 를 못 박고」)의 축자이고 주석 스스로 「원장에 등재돼 있다」고 적는다. 4행째를 「서버가
  승인을 받은 뒤에 한다.」로 잘라 재작성(−2, 재작성 1).
- `DecideDiff.tsx` 「첫 탭은 사유 칸을 연다 — 사유 없는 거부는 다음 제안이 무엇을 피할지 말해 주지
  못하므로 서버도 받지 않는다」 2행 — ① 바로 아래 `if (!rejecting || reason.trim().length === 0)
  { setRejecting(true); return; }` · 0009 정본 · `decide()` 오류 문자열.
- `RequestEdit.tsx` 는 제거 0. 머리 「무엇을 고칠지(대상)와 무엇을 피해야 하는지(거부 이력)는 둘 다
  서버가 준다 — 화면이 기억하는 것은 사람이 지금 치고 있는 문장뿐이다」 2행은 화면 불변식(유지).
  「보내기 전에 화면이 막는 이유는 서버가 같은 요청을 거절하기 때문이 아니라, 빈 요청으로 사람의
  키를 쓰는 호출을 하지 않기 위해서다」 2행 — **판단이 갈려 남긴 것**(3건째): 서버 `propose()` 는
  빈 요청을 `active_key_for_user` **전에** 거절하므로 「키를 쓰는 호출」이라는 사유는 서버 코드와
  어긋나 보이나, 이 문장은 어디에도 되풀이돼 있지 않아 「되풀이된 주석이 낡아 틀린」 유형이 아니고
  정확성은 판정 표면 밖이다. 적혀 있는 이유가 유일한 지식일 가능성(예: 화면이 막지 않으면
  세션 쿠키를 실은 POST 가 나간다는 뜻)을 배제할 수 없어 남긴다.

## 증분 — 판정 완료 원장 행 안의 15행 (원장 1·2·5·10행)

각 원래 패스 파일에 절을 더했다: [2026-09-17-backend-concentrated.md](2026-09-17-backend-concentrated.md)
「증분 재판정 ⑥」(원장 1행 `analysis.rs` 4행 → 제거 4) · [2026-09-18-uncontested-harness-config.md](2026-09-18-uncontested-harness-config.md)
「증분 재판정 ②」(원장 2행 `config.rs` 2행 → 제거 2) · [2026-09-18-worker-double-axis.md](2026-09-18-worker-double-axis.md)
「증분 재판정 ②」(원장 5행 `deploy/e2e/kustomization.yaml` 2행 → 제거 2) ·
[2026-09-20-frontend-shell-axis.md](2026-09-20-frontend-shell-axis.md) 「증분 재판정 ③」(원장 10행
`api.ts` 5행 → 제거 3 · 유지 2, `App.tsx` 2행 → 제거 2). 1·2·5행은 줄 수·지문이 #92 이전 값으로
**바이트 동일하게 되돌아왔고**(590/`9757b6b5…` · 140/`ddacce0b…` · 190/`632b0475…` — 부모
`19d58fa` 재계산과 동일; #92 가 그 행들에 더한 것이 전건 제거된 주석뿐이기 때문), 10행은 `api.ts`
유지 2행만큼 늘어 **83 / `ec54e678…`** 이다.

## 검증

- **판정기**: `python3 tools/check-data-format-change.py --base 51daa9c --head <준비 브랜치 head>
  --verbose` → **`✅ 변경 없음`**(검사 파일 10/10 — D1·D2·D5·D6 어느 경로에도 안 걸린다).
  감지가 1줄 probe 로 잰 것을 실제 후보 트리에서 다시 돌린 것이다(「슬라이스 전 필수 절차」).
- **주석 제거 후 부모와 바이트 동일 10/10** — 줄머리 `//`·`///`·`//!`·`#`·JSDoc 과 블록 주석을 걷어낸
  잔여의 md5 가 `51daa9c` 와 같다(`analysis.rs` `10548c24` · `config.rs` `057df9b5` · `doc_edit.rs`
  `8d4f7623` · `tests/doc_edit.rs` `88e278d9` · `kustomization.yaml` `6d2a26da` · `sc03-01` `899311b9` ·
  `sc03-02` `fbae0df7` · `App.tsx` `2fa2b7b0` · `DecideDiff.tsx` `5d6c33d8` · `api.ts` `2995df01`).
  「비주석 diff 0줄」은 쓰지 않았다(10차 패스의 경고).
- 문서 게이트 4종 rc=0(`check-scenario-e2e` · `check-mockup-render` · `check-journey-mockup` ·
  `check-journey-prototype`) · `npm run build`(tsc + vite) rc=0. `cargo test` 와 kind e2e 는 이 호스트에
  Rust 가 없어 CI 가 집행자다 — Rust 쪽 변경은 `///`·`//!`·`//` 줄 삭제뿐이라 컴파일 표면을 건드리지
  않는다(stripper 동일).
- 전역 지문: 부모(= 트리거) `lines=2504 files=115` / `8250b3cb…` → **`lines=2404 files=115` /
  `543ccf5f…`** — 순 제거 **100**, 파일 수 불변(주석 0행이 된 파일 없음 — `tests/doc_edit.rs` 는 1행이
  남는다).
- 원장 산술(준비 시점 트리, base `c9398e6`): 행 열 합 2,076 + 새 행 56 = **판정 완료 2,132** · 미판정 증분 **0** ·
  잔여 = 2,404 − 2,132 = **272** = 전역 파일 목록에서 원장 104파일을 집합으로 뺀 15파일(마이그레이션 9 / 169 ·
  D2 4 / 44 · D6·D5 2 / 59)의 행수 합과 일치.
- **16차 패스(#98, `0d2574c`)가 준비 중 먼저 착지했다.** 두 PR 은 코드 파일 겹침 0 이고 원장·허브만 함께 고쳤으므로
  `origin/main` 을 이 브랜치로 머지(force-push 없음)해 두 행·두 허브 항목·`Documents` +2 를 살리고, 합계는 **머지
  트리에서 재실측**했다: 전역 **`lines=2389 files=115` / `33061a66…`**(#98 착지 tip 2,489 − 100) · 행 열 합 2,255 =
  판정 완료(16차 행 123 / `1b35c33a…` 재현 · 이 패스의 새 행·1·2·5·10행 값 불변) · 잔여 **7파일 / 134**(0009 30 · D2
  4 / 45 — `migrations.rs` 는 16차의 +1 로 14 · D6·D5 2 / 59) == 전역 − 판정 완료 == 집합 뺄셈. 순 제거 100 은
  두 트리에서 같다.

## 범위 밖 (후속)

- **`backend/migrations/0009_feature_doc_edits.sql` 30행** — 사람 게이트(본문 「적용된 마이그레이션」).
  파킹된 PR #98(0001~0008)의 범위 밖이므로, 그 task 재개 시 0009 를 같은 repair 에 합칠지(repair 는
  1회 비용이라 합치는 쪽이 싸다) 별도 패스로 둘지는 그때의 계획이 정한다. 0009 는 #92 가 머지 전에
  마이그레이션 README 의 작성 규칙에 맞춰 이미 한 번 고쳤다(`9b36a98`) — 판정 대상이긴 하나 이
  패스가 정본으로 삼은 문단들이 거기 있다.
- **이미 판정된 spec 12개의 Isolation 블록**(`sc01-01`·`sc01-04`·`sc01-06`·`sc01-07`·`sc02-01`·
  `sc02-04`·`sc02-05`·`sc02-06`·`sc02-07`·`sc02-08` 의 4~20행 블록, 그리고 5차 패스가 판단 갈림으로
  유지한 `sc04-07`·`sc04-08` 의 긴 모양) — 13차 패스가 적어 둔 후속 후보 그대로. 원장 3·4·5·6·7·8행에
  걸치는 증분 재판정이라 한 패스로 모아서 해야 한다.
- 잔여 D2 4파일/44행 · D6·D5 2파일/59행 — 사람 게이트, 변동 없음.

## 증분 재판정 ① — `#107` 이 `doc_edit.rs` 에 더한 1행 (2026-09-21 · `rct_20260921-0010`)

`#107`(`89a1625`, 슬라이스 6b)이 `document_of` 에 `crate::feature_add::overlay(state, analysis_id, &mut doc).await?;`
를 끼우며 이 패스가 유지한 인라인 주석 「고칠 대상도, 그 위에 얹을 제안도 **사람이 지금 보는 문장**
기준이어야 한다」에 「— 사람이 직접 더한 feature(AC3.2)까지 포함해서」 1행을 덧붙였다. 덧붙인 절을 판정해
**제거**하고 문장을 #107 이전 한 줄로 되돌렸다 — ① 바로 아래 새 호출 자체 · `feature_add::overlay` 의 doc
(순서 계약의 정본) · ② doc-tracker 6b 행 「더해진 feature 도 AC3.1 편집의 대상이다」 — AC 꼬리표.

결과: 17행의 줄 수·지문이 #107 이전 값 **56 / `b9e6778d…`** 으로 되돌아왔다(부모 `24f488d` 재계산과
바이트 동일). `doc_edit.rs` 는 주석 제거 후 부모와 바이트 동일(stripper md5 `c9a1e102`). 새 파일 쪽 판정은
[2026-09-21-feature-add-axis.md](2026-09-21-feature-add-axis.md).

## 증분 재판정 ② — `#108` 이 `doc_edit.rs` 에 더한 1행 (2026-09-22 · `rct_20260922-0001`)

사람 PR **#108**(AC4.9 출력 언어 설정)이 `backend/src/doc_edit.rs::propose` 에 1행을 들여왔다:
`// 고친 문장은 문서의 나머지와 같은 언어여야 한다 — 사용자의 지금 설정이 아니라 분석의 언어.`
(바로 아래 `let language = crate::settings::analysis_language(&state.db, &id).await?;`)

**제거 1.** 「사용자의 지금 설정이 아니라 분석의 언어」는 호출하는 함수 이름 `analysis_language` 가 그대로
말하고(①), 「문서의 나머지와 같은 언어여야 한다」는 그 함수의 doc 이 **이 축의 정본**으로 적는다 — 「what every
LLM call made *for* that analysis writes in, including the ones the API makes directly (edit proposals, manual
feature drafts), so their prose matches the document it lands in」. 같은 자리가 `feature_add::draft` 에도 한 벌
있었고 같은 판정으로 걷었다(원장 18행). 판정 맥락은
[2026-09-22-output-language-axis.md](2026-09-22-output-language-axis.md).

지문: **57행 `8f9cc1d6…` → 56행 `b9e6778dd2e3db73903e12298c68916799605427c5d530a8ffca4ee471425054`** —
**이 패스 직전 원장 값으로 바이트 그대로 복귀**했다.

## 증분 재판정 ④ — `#152` 가 `sc03-01` 「탭 3」 뒤에 더한 3행 (2026-09-25 · `rct_20260925-0007`, 30차 패스)

자매 모델의 PR **#152**(`ea04fe0`, AC4.4 슬라이스 7a)가 이 범위의
`e2e/tests/sc03-01-llm-assisted-edit.spec.ts` 에 3행을 들여왔다. AC4.4 의 접힘이 390px 에서 인수 시나리오
절을 접으므로, 승인 뒤 목록을 보려면 요약을 한 번 펼쳐야 해서 생긴 블록이다.

```
      // 여기서부터는 결과 확인이라 탭 계수 밖이다. 390px 에서는 AC4.4 에 따라 인수
      // 시나리오 절이 접힌 채 서므로, 목록을 보려면 요약을 한 번 펼쳐야 한다 —
      // 세는 탭 셋(`request-edit`·`send-request`·`approve-diff`)은 위에서 이미 끝났다.
```

**순 제거 2 · 유지 1(재작성).**

| 절 | 판정 | 근거 |
|---|---|---|
| 「390px 에서는 AC4.4 에 따라 인수 시나리오 절이 접힌 채 서므로, 목록을 보려면 요약을 한 번 펼쳐야 한다」 | 제거 | **AC 조항 재진술**(② — PRD AC4.4 · 테스트 문서 `04-platform.md#시나리오 6` 기대 결과)이고, 바로 아래 `getByTestId('scenarios-disclosure').locator('summary').click()` 세 줄이 그 문장 자체다(①) |
| 「세는 탭 셋(`request-edit`·`send-request`·`approve-diff`)은 위에서 이미 끝났다」 | 제거 | 위쪽 `── 탭 1 ──`·`── 탭 2 ──`·`── 탭 3 ──` 표식 바로 밑의 세 클릭이 **정확히 그 세 testid** 다 ⇒ ① 의 열거. 표식 3행은 이 파일 1차 판정이 「판단이 갈려 남긴 것」으로 유지해 둔 자리이므로 정본이 이미 있다 |
| 「여기서부터는 결과 확인이라 탭 계수 밖이다」 | **유지**(재작성) | 파일 머리의 **유지된 정본**은 「진입부터 승인까지는 화면의 버튼만 누르고, 결과 확인만 저장된 문서를 읽는다」다. 이 블록은 승인 **뒤에** 버튼을 누르므로 그 문장과 **겉으로 어긋난다** — 그 어긋남이 규칙 위반이 아니라는 화해는 네 경로 어디에도 없다. 없으면 다음 편집자가 이 클릭을 네 번째 탭으로 세거나 「탭 3」 위로 올려 「3탭 이내」 단정의 의미를 조용히 깬다 |

남긴 1행:

```
      // 아래 펼침은 결과 확인이라 탭 계수 밖이다 — 세는 탭 셋은 「탭 3」 에서 끝났다.
```

③ 는 이 자리를 닫지 않는다. PR #152 재작업 절이 같은 말을 적지만(「세는 탭 셋은 L65 에서 끝나고 그 뒤는
결과 확인 구간이라 「3탭 이내」 단정의 의미가 보존된다」) 그것이 막는 편집은 **이 파일 안**에서 일어난다.

### 값

58(기재 · 부모 `f4b7db3` 에서 바이트 일치) → 61(유입 후) → **59 /
`d0a019a9654ce08249f6d7b03a831aeadd0f4167dab23b090151d075e5b6bec3`**(판정 후).
`sc03-01` 은 주석 줄을 걷어낸 잔여가 부모와 **바이트 동일**(비주석 diff 0줄) · 단언 건수 불변 ·
기계 판독 `// 검증 시나리오:` 1개 보존. `check-scenario-e2e.py` rc=0.

## ④ 축 판정 — `doc_edit` 축 6파일 (2026-09-26 · `rct_20260926-0009`, 42차 패스)

이 행은 2026-09-21 원 판정과 증분 ①~④ 로 ①②③ 을 마치고 **④ 만 남겨** 두었다. 42차 패스가 그 한 축을
채운다. 슬라이스는 예산 400줄을 채우며 이 덩어리를 「④ 만 남은 덩어리 중 큰 것」으로 집었다.

### 모집단 — 저작 커밋 8개, 전부 제목 한 줄

`git log --format='%s' -- <이 행의 6파일>` 은 13개를 돌려주고, 그중 **주석 비중복성 판정 패스 5건**은
④ 의 모집단이 아니다(판정 패스가 「유지」를 설명하려 인용한 문면을 ③·④ 히트로 세면 정책이 지키라는
주석이 전부 제거 대상이 되는 자기 무효화 고리가 생긴다). 남은 저작 커밋 **8개**:

`#92`(슬라이스 6a — AC3.1 LLM 보조 문서 수정) · `#107`(AC3.2 feature 문서의 추가 — 슬라이스 6b) ·
`#112`(AC3.3 삭제와 보존 — 6c) · `#114`(AC3.5 충돌 처리 — 6d) · `#126`(AC3.4 변경 이력·임의 시점
복원 — 6e) · `#108`(AC4.9 출력 언어 설정) · `#152`(AC4.4 요약 → 상세 — 7a) · `#176`(게이트 M10).

**여덟 전부 squash 제목 한 줄이고 `Co-authored-by` 트레일러 밖 본문이 0자다**(실측). 그래서 ④ 가
복원할 수 있는 것은 제목이 담은 것 — AC 번호 · 슬라이스 꼬리표 · task id · PR 번호 — 뿐이다.

### 실측 — 최장 연속 일치 8자

잔여 **59행 전건**(`doc_edit.rs` 31 · `tests/doc_edit.rs` 1 · `sc03-01` 10 · `sc03-02` 7 ·
`DecideDiff.tsx` 4 · `RequestEdit.tsx` 6)을 제목 집합과 대조했다.

| 최장 | 조각 | 그 줄 |
|---|---|---|
| **8자** | ` LLM 보조 ` | `//! LLM 보조 수정(AC3.1)과 그 변경의 출처 보존(AC3.4) — 라우터를 그대로 돌려 본다.` |
| 7자 | `feature` | `// docs/mockups/JRN-review-feature.html#STP-request-edit 의 구현.` |
| 6자 | ` AC3.1` | `//! AC3.1: 읽던 사람이 한 줄로 부탁하면 그 시나리오를 고쳐 주는 흐름.` |

8자 히트는 제목의 `AC3.1 LLM 보조 문서 수정` 과 주석의 `LLM 보조 수정(AC3.1)` 이 겹친 부분이지만,
그 주석 줄이 말하는 것은 「**라우터를 그대로 돌려 본다**」(테스트의 방법)이고 제목에는 없다. `feature`
·`AC3.1` 은 경로·식별자 조각이다. **되풀이된 명제는 0건**이다.

### 판정

**④ 는 이 범위에서 공전이다 — 제거 0행.** 남은 59행은 ①②③ 에서 이미 판정된 그대로이고, ④ 가 새로
걷을 줄이 없다. 줄 수·지문 **불변**
(59 / `d0a019a9654ce08249f6d7b03a831aeadd0f4167dab23b090151d075e5b6bec3`) · **파일 무접촉**.
판정 축 `①②③` → **`①②③④`**.

⚠️ 이 결론은 **이 레포의 머지 방식에 묶인다** — squash 머지라 본문이 트레일러뿐이다. 머지 방식이
바뀌어 커밋 본문이 실리기 시작하면 이 행의 ④ 는 그 시점부터 다시 물어야 한다.

---

## 원장에서 옮겨 온 증분 재판정 기록 (2026-09-26 형식 이전)

아래는 `ledger.md`의 결과 칸에 쌓여 있던 증분 재판정·정정 기록을 **문면 그대로** 옮긴
것이다. 형식 이전(템플릿 「원장 형식」)이 원장에 표와 「읽는 법」만 두기로 하면서, 각 행의
경위는 그 행의 패스 파일로 돌아왔다. 옮기면서 한 글자도 고치지 않았고 판정을 새로 하지
않았다 — 행을 가리키는 순번도 당시 표기 그대로다.

### 원장 행 17 — `backend/src/doc_edit.rs` · `backend/tests/doc_edit.rs` · `e2e/tests/sc03-01-llm-assisted-edit.spec.ts` · `e2e/tests/sc03-02-rejected-suggestion-avoided.spec.ts` · `frontend/src/DecideDiff.tsx` · `frontend/src/RequestEdit.tsx` (문서 편집 축 — 슬라이스 6a #92 가 들여온 새 파일 6개)

**증분 재판정 ①**(2026-09-21): #107 이 `doc_edit.rs` `document_of` 의 인라인 주석에 덧붙인 1행(「사람이 직접 더한 feature(AC3.2)까지 포함해서」 — 바로 아래 `feature_add::overlay` 호출과 그 doc 이 정본)을 **제거** · 줄 수·지문은 #107 이전 값 56/`b9e6778d…` 으로 바이트 동일 복귀 — [passes/2026-09-21-doc-edit-axis.md](2026-09-21-doc-edit-axis.md) 「증분 재판정 ①」

### 원장 행 17 — `backend/src/doc_edit.rs` · `backend/tests/doc_edit.rs` · `e2e/tests/sc03-01-llm-assisted-edit.spec.ts` · `e2e/tests/sc03-02-rejected-suggestion-avoided.spec.ts` · `frontend/src/DecideDiff.tsx` · `frontend/src/RequestEdit.tsx` (문서 편집 축 — 슬라이스 6a #92 가 들여온 새 파일 6개)

**증분 재판정 ②**(2026-09-22): #108 이 `doc_edit.rs::propose` 에 더한 1행(「고친 문장은 문서의 나머지와 같은 언어 — 사용자의 지금 설정이 아니라 분석의 언어」 — 호출하는 `settings::analysis_language` 의 이름과 그 doc 이 정본)을 **제거** · 줄 수·지문은 #108 이전 값 56/`b9e6778d…` 으로 바이트 동일 복귀 — [passes/2026-09-21-doc-edit-axis.md](2026-09-21-doc-edit-axis.md) 「증분 재판정 ②」 · 맥락 [passes/2026-09-22-output-language-axis.md](2026-09-22-output-language-axis.md)

### 원장 행 17 — `backend/src/doc_edit.rs` · `backend/tests/doc_edit.rs` · `e2e/tests/sc03-01-llm-assisted-edit.spec.ts` · `e2e/tests/sc03-02-rejected-suggestion-avoided.spec.ts` · `frontend/src/DecideDiff.tsx` · `frontend/src/RequestEdit.tsx` (문서 편집 축 — 슬라이스 6a #92 가 들여온 새 파일 6개)

**증분 재판정 ④**(2026-09-25, 30차 패스): #152 가 `sc03-01` 「탭 3」 뒤에 연 3행을 판정해 **순 제거 2 · 유지 1(재작성)** — 「390px 에서는 AC4.4 에 따라 인수 시나리오 절이 접힌 채 서므로 목록을 보려면 요약을 한 번 펼쳐야 한다」는 AC4.4 재진술(②)이고 바로 아래 `scenarios-disclosure` → `summary` 클릭이 그 문장 자체(①) · 「세는 탭 셋(`request-edit`·`send-request`·`approve-diff`)은 위에서 이미 끝났다」는 위 세 `── 탭 N ──` 표식과 같은 testid 열거라 ① · **유지 1행**은 파일 머리의 유지된 정본(「결과 확인만 저장된 문서를 읽는다」)과 승인 **뒤에** 버튼을 누르는 이 블록을 화해시키는 「탭 계수 밖」 한 문장이다 — 그것이 없으면 다음 편집자가 이 클릭을 네 번째 탭으로 세거나 「탭 3」 위로 올려 단정의 의미를 조용히 깬다 — [passes/2026-09-21-doc-edit-axis.md](2026-09-21-doc-edit-axis.md) 「증분 재판정 ④」

