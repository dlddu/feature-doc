apiVersion: v1
kind: Service
metadata:
  name: featuredoc
  labels:
    app.kubernetes.io/name: featuredoc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: featuredoc
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
