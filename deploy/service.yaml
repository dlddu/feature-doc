apiVersion: v1
kind: Service
metadata:
  name: featuredoc-hello
  labels:
    app: featuredoc-hello
spec:
  type: ClusterIP
  selector:
    app: featuredoc-hello
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
